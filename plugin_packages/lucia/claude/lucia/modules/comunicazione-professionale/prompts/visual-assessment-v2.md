# Professional communication visual assessment v2

In a fresh host session, inspect every exact rendered PNG and every rendered
document page represented by the current manifest. Compare the carousel with
the accepted channel copy. Assess information gain, sequence, readability,
identity placement, public source readability, pagination, clipping, and
Studio-format fidelity. Treat Creative Production only as art-direction
provenance; judge Vera's exact deterministic output.

Return JSON matching `visual_assessment.schema.json`. Cover every slide and
every rendered document once. A weak or redundant slide, illegible document,
silent omission, broken pagination, or unfaithful Studio identity prevents a
`ready` verdict. Do not read the generation or editorial transcripts.
