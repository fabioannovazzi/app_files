---
name: studio-archive
description: Use when Lucia must create or resume a durable client engagement, import selected inputs, and prepare or start a client-bound workflow through the Studio Archive component shared with Vera.
---

<!-- LUCIA_OPENAI_ONBOARDING_BEGIN -->
Before the first substantive action, read `../lucia/references/local-onboarding.md`.
Check the shared OS-user local profile even on direct specialist invocation.
A verified native working-chat handoff executes only its bound lesson and token.
During a tutorial all profile, progress, examples and feedback remain local;
never send a change request, stamp a tutorial receipt or call hosted interviews.
This learning gate takes precedence over ordinary feedback and cloud fallback.
<!-- LUCIA_OPENAI_ONBOARDING_END -->

# Archivio dello studio

After substantive use of this workflow, read and follow the `Plugin Improvement Feedback` section in `../lucia/SKILL.md`.

Resolve `../../modules/studio-archive` from this skill directory when it
exists; otherwise resolve `../../../studio-archive` in the repository. Read
that module's `skills/studio-archive/SKILL.md` completely and follow it.
Treat the resolved module root as the plugin working directory for every
command. Do not paraphrase, shorten, fork, or replace the component workflow.

Use `luciaStudioArchive` for the shared MCP tools. Its host configuration
also enables Lucia's `apertura-pratica` lifecycle. The component's `Vera/`
ledger paths and private state names remain the shared storage contract.

Lucia's public experience and deliverables are in Italian.
