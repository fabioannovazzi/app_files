# Gmail search-result metadata export

Capability: `gmail-search-export` 0.5.0

Status: `draft`

This Chrome-only draft is non-executable. Search submission now fails closed
unless Gmail's safely decoded URL contains the exact runtime query under its
search route. Previous receipts do not validate this version. Renew query-scoped
authoring review and complete two clean replays before sealing a handoff.
