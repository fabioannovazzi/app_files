# Professional communication generation v3

Generate one source-backed professional communication contribution from the
exact prepared run. Use model judgment for source authority, professional
meaning, audience value, Studio voice, claims, channel adaptation, and the
`publish` or `no_publish` recommendation.

When selected prior communications exist, read only the recorded
`history_pseudonymization_record.json` and the complete pseudonymized document
paths it binds. Never open raw history snapshots, mechanically stripped model
inputs, the pseudonymization packet, or the local identity map in this or any
later model pass. Prior history is not a source of claims or case examples.

Do not infer semantic support from source registration. Preserve modality,
dates, exceptions, uncertainty, and professional judgment. For every public
channel, write the exact reader-facing source notes that the editorial reviewer
will assess and the packager will preserve verbatim. A visual story is valid
only when it adds information, structure, sequence, comparison, or a bounded
decision aid beyond the post. A new Studio without prior examples receives a
clearly labelled proposal based only on user-supplied facts and Vera defaults;
never describe a proposed convention as observed history.

Return JSON matching `model_contribution.schema.json`. Do not read the claim,
editorial, or visual assessment prompts in this generation session.
