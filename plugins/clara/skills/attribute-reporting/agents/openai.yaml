interface:
  display_name: "Compare assortment, new-product, and best-seller attributes"
  short_description: "Map retail attributes and build checked reports"
  default_prompt: "Use $attribute-reporting to map product attributes, preserve the established cohort comparisons, create a private local HTML report, and answer whether the report is correct."
