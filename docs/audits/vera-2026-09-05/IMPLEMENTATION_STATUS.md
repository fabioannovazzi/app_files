# Vera remediation implementation — 5 September 2026

The user authorized local implementation of T01–T20. The goal is active, not complete:
implementation and technical checks do not establish independent professional
acceptance or qualification of an unsupported host. No deployment, merge or
publication has been performed by this task.

## Current checkpoint — 6 September 2026

Latest resumed implementation: see `RESUMED_IMPLEMENTATION.md`. Newly
reproduced corrections cover Variance nonfinite tolerances, Open Item doubled
source-map counts, stale Business Planning conclusions on blocked outputs,
Bandi response-to-packet binding, HTML attribute leakage in source extraction,
pending commercial inputs labelled accepted, the XML guide's row-count wording,
mixed-PDF page coverage, exact SARI official-host handling, UTF-16 OOXML entity
protection, and three Passive Invoice numeric errors.
`SAMPLING_ACCEPTANCE_MAP.md` now records all six named T03 cases through managed
observations. Ten Management Control cases and six XML consumer variants have
additional bounded public-output evidence; their delivery/visual limits remain
explicit. A fresh Management Control run has also sealed its 20-artifact delivery.
Current local Vera package identities are in `passive-014-identities.json`
under the external evidence root, including the current dirty inventory.
Fresh Bandi source-only contributions are retained, including one excluded
scope-error attempt and its correctly scoped fresh-session replacement.
The concise Italian source proposal collection is `BANDI_CONTRIBUTI_RIPRESA_IT.md`.

Resumption: the user explicitly authorized fresh Astra Medium subagents and
requested restart. The goal tool confirms `active`. Fresh source interpretation
and independent acceptance checks have produced further verified results. Missing historical
benchmark PDFs constrain only their specific Centrale Rischi cases; host tests
and source-to-output review remain individual verification items, not a blanket
stop on implementation. Existing approvals of the Italian expectations remain valid.

The full T01–T20 goal remains incomplete. User approval of the proposed fixes and
Italian review expectations is already recorded; it must not be requested again.
That authorization is separate from evidence that every workflow passed its
professional and host acceptance cases.

The user confirmed that this task is now set to Astra Medium and explicitly
requested resumption after the diagnostic handoff. Remediation has resumed;
do not request the model-setting handoff again.

- Chrome is connected through Settings → Computer Use → Google Chrome. Source
  skills, wrapper and privacy instructions now use that entry point. The shipped
  fixture completed through its documented Playwright API. Current download-byte
  evidence, portable replay and changed-page/login/origin qualification remain
  distinct outstanding checks; absence of a separate Chrome plugin is not a
  blocker.
- Vera Codex, Cowork and Platform-upload packages were rebuilt and each passed
  its source consistency check. Packaged MCP startup/tool listing passed for all
  18 servers. These are local build results, not publication evidence.
- Latest package/update suite: 387/390 passed, no skips. The two extracted Clara
  render cases subsequently passed with approved network access (2/2); Clara's
  public manifest 0.1.177 still trails installed 0.1.180. Local source/ZIP parity
  checks pass. The regenerated shared Claude catalog also passes its parity test. Do not
  infer publication from the installed cache or alter public version metadata
  without verified publication and deployment authorization.
- Sales Plan 0.1.7 corrects missing discounts becoming zero and incomplete
  sums appearing as totals. The managed before/after case, 29 passing tests,
  86.06% engine coverage and isolated regression-detection checks support the
  fix. Three Vera packages match source; current identities are retained in
  `sales-017-final-identities.json` outside the checkout.
- Full journal-sampling suite: 171 passed, no skips. Separate new regressions
  pass for stratified capacity boundaries and repeated monetary-unit hits.
  Managed intake plus new disposition regressions: 22 passed. Archive component
  and wrapper: 71 passed, including real temporary-file recovery checks.
- Local XBRL qualification now includes 24 official-taxonomy gold cases and
  606/606 official conformance cases. Centrale Rischi's exact synthetic gold PDF
  passed its numeric benchmark. Two recovered exact-hash official guides now
  pass 15 extraction/control cases and 10 analysis cases. The historical and
  webinar PDFs remain missing; independent professional assessment is open. AML successor output hashes and
  predecessor preservation passed; structural readiness is not professional
  approval.
- Integrated session 48596 finished with exit 1: 9,532 passed, 103 failed,
  19 errors, 34 skipped; coverage 79.50%, below 80%. It spans subsequent edits
  and is a baseline diagnostic, not final unchanged-source qualification.
  See INTEGRATED_DIAGNOSTIC.md. Post-handoff batches pass 26 accounting/security
  tests and 49 workbench audit tests. Replay of all 122 original failed/errored
  cases produced 84 passes, 38 failures and no errors/skips; the catalog and two
  network-dependent render failures have separately passed since. This subset
  does not update full-suite coverage. Completion still requires resolving the
  remaining failures and independently closing workflow, professional, host,
  and package requirements. Do not poll or restart the completed session.
- Plugin Mypy passes the configured 21-file check with Sales Plan now included.
  The expanded Bandit scan has no medium/high findings. Broader formatting and unsuppressed repository typing findings
  remain recorded below. Full quality-gate closure is not established.

Remaining acceptance spans T08/T10 host behavior, T13 justified decomposition,
T14–T18 independent source-to-output/professional outcomes, T19 browser changed
states, and T20 integrated gates and exact artifact identity. The original
[handoff requirements](VERA_REMEDIATION_HANDOFF.md) remain authoritative; these
results do not shrink their scope. Detailed dated entries below retain failures,
source paths and receipt locations. No merge, deployment or publication occurred.

## Historical checkpoints — superseded where later evidence differs

**Latest A08 result (6 September):** Changed sealed inputs and copied prior
review rows are rejected. A legitimately imported EUR 110 replacement requires
a fresh review and leaves the EUR 100 invoice unresolved, with EUR 10 shown as
an unexplained difference. The new reviewed successor is `final_ready`.
All 277 Open Item tests pass. A further A08 run now verifies replacement history
through the existing sealed run purpose and exact input manifests: the old run,
old input, new input and reason for re-review remain available from `list_runs`.
Its reviewed successor is `final_ready`; missing and wrong predecessor
checkpoints are rejected, and the correct checkpoint passes isolated replay.
This is explicit recorded history, not an inferred or typed replacement graph.
Evidence: `source-acceptance-a08-linked-history/history-verification.json`,
`final-verification.json` and `isolated-successor-replay.json` under the retained
temporary evidence directory.

**Prior A02 result (6 September):** The reversal case now retains +1220/-1220
and the unexplained payment without allocating it. Its reviewed successor is
`final_ready`; reversal validity remains an explicit evidence request. All 276
Open Item tests pass. See the source-intake finding for the exact evidence.

**Prior A07 review result (6 September):** A07 now completes source intake, local
review application and normal successor regeneration with all checks passing
and `final_ready`. Both invoices remain unresolved and no allocation is made.
The full Open Item suite passes 271 tests; both Vera package drift checks pass.
A03 now retains both imported filenames in normal Word/Excel reports while
counting one EUR 1220 invoice. Its fresh reviewed successor is `final_ready`.
Archive/reconciliation tests: 396 passes; shared-assurance core: 93 passes.
The optional shared context field invalidates additional consumer privacy
fingerprints; the full privacy register is not yet current.
The real run exposed and verified the predecessor-snapshot customer-context
correction. See [SOURCE_INTAKE_FINDING.md](SOURCE_INTAKE_FINDING.md) for evidence
and the distinction between Codex acceptance and independent professional review.

**Prior integration state (6 September):** A04 grouped-payment correction is
verified (267 module tests plus 11 workflow reruns). One bank movement supplies
both allocations without duplicated capacity. Seven privacy records became
stale after concurrent changes; the prior complete-register pass below is a
historical checkpoint. See the final A04 section for the exact affected records.

**Source-intake correction:** Open Item now reads the nested Studio Archive
input tree and retains full relative source identities. A fresh real archive
import → PDF extraction → reconciliation → Word/Excel/assurance run returns
EUR 488 allocated and EUR 732 residual. The only failing check is the still-
pending `codex_review_completed`; source processing and document finalization
are no longer blocked. See [SOURCE_INTAKE_FINDING.md](SOURCE_INTAKE_FINDING.md).
The source-decision JSON/prose defect and bounded multi-line PDF locator are
also corrected. 263 module tests pass, plus the subsequently added real-PDF
same-basename regression (three nested tests pass together).

- A01 partial-payment defect is corrected and retained reports show EUR 488.00
  allocated and EUR 732.00 outstanding against EUR 1220.00. All 259 Open Item
  tests pass, including document assurance and negative allocation cases.
- User approval of all 16 Italian proposals is recorded. Additional approval
  of those same expectations is not pending. Full source-intake executions
  and independent outcome measurements remain incomplete; credentials were
  not independently verified.
- Five stale privacy records were reviewed against the current source changes.
  Their controls now explicitly distinguish inert cache exclusion from source
  execution authority and document the bounded optional vendor-cache repair.
  Report Builder also documents its exact full/projected host-layout contract.
  The complete Vera privacy register validates as current. This is an
  engineering boundary review, not professional or legal certification.
- Current local candidates read directly from manifests: Vera 0.1.206,
  Clara 0.1.180, Lucia 0.1.26. The last directly observed Published versions
  are Vera 0.1.205, Clara 0.1.177, Lucia 0.1.28; publication is a separate
  evidence layer. Lucia candidate parity remains outside this Vera fix.
- The six synthetic model calls, two successful local browser replays and
  dependency-corrected extracted chart runs remain valid for their recorded
  snapshots. They do not establish native host or full professional acceptance.
- T01–T20 remain the objective. Pending qualification includes source-to-final
  accounting cases, financial claim/source and visual cases, professional
  source cases, extraction/AML cases, changed-browser-state cases, supported
  native execution and final integrated gates. These have not been waived.

The sections below retain dated investigation history. Statements such as
“approval pending”, “partial payment incomplete”, old publication observations,
or “six stale privacy records” describe earlier checkpoints only.

## Approved experiments and Italian reviewer handoff

Follow-up correction: the Word cutoff summary omitted the source record IDs
behind its evidence totals. Added those references to the code-owned summary;
both the normalized partial-payment and ambiguous-payment examples now seal
their documents successfully. The ambiguous case leaves both invoices
unresolved, without selecting an unsupported match. Workflow/output regression
batch: 15 passes. Added a public partial-payment document-lineage regression.
These are source changes pending final package/privacy reconciliation.

The partial-payment economic outcome is still incomplete: its row remains
unresolved with supporting bank amount 488 rather than explicitly reporting
allocated 488 and residual 732. Successful document sealing does not close A01.
Retained outputs: `partial-evidence-repair/` and `ambiguous-evidence-repair/`
under the temporary evidence directory. No additional user approval is needed
for the remaining authorized fixes.

The user explicitly approved all proposals in `REVISIONE_PROFESSIONALE_IT.md`.
Recorded the exact message, document hash and verified accounting-source hashes
in `/private/tmp/vera-remediation-01a07083/professional-proposals-user-approval.json`.
Reviewer credentials are not independently established; this is an authentic
conversation approval of the expected proposals, not completed runtime evidence.

Initial normalized Open Item evaluation of A01/A05/A06/A07 uncovered a display
parser defect: grouped `1,220.00` could be read as 220 during artifact sealing.
The parser now consumes the full grouped token, including the equivalent
European representation. A public workflow regression is added. Follow-up
normalized runs remain incomplete: A05 produced an unresolved item as expected;
A06 rejected currency-mismatched relationship controls; A01 and A07 still fail
artifact formula attribution for evidence amounts. These failures are retained
in `approved-normalized-cases-v2/results.json`; no full-pipeline pass is claimed.
Source-intake/Studio Archive, final review and the other cases remain to execute.

The user subsequently authorized the production dependency correction. The
missing NumPy/psutil/dateutil/xlsxwriter declarations and preflight checks are
now corrected across the five affected chart components. Both actual packaged
render reruns pass; Distribution's normal PNG output is verified instead of
requiring fallback HTML. See `RENDER_DEPENDENCY_FINDING.md` for exact evidence.
Both Clara/Vera Codex and Cowork packages were rebuilt and pass drift checks;
Vera's 18 MCP startup checks pass. A separate current Vera privacy validation
reports stale records for six accounting/report workflows; those concurrent
source changes have not been blindly approved or refreshed by this correction.
The dependency approval blocker below is historical and resolved.

Latest Clara source reconciliation supersedes the version-gap note below:
reviewed all ten Clara paths differing from origin/main, whose manifest was
0.1.179. Integrated its managed-module render launcher, Business Planning
dependency preflight entry and mechanical-acceptance currency documentation.
Retained the equivalent local currency CLI wording, reviewed atomic runtime
implementation and local privacy records. Candidate Clara is now 0.1.180;
published announcement remains the observed 0.1.177. Refreshed only affected
reporting-engine, Clara and update-service privacy fingerprints. The focused
reporting/notification/privacy suite passes all 69 tests. Codex and Cowork ZIPs
were rebuilt and both source-drift checks pass. These new package identities
supersede earlier Clara 0.1.173 checkpoint hashes; earlier runtime renders
remain evidence for their recorded snapshots, not fresh 0.1.180 execution.

The subsequent 390-test package/notification batch passed 388 tests and failed
two actual extracted-package renders. Initial failures were sandbox DNS access
to PyPI. An approved network retry completed but both renders failed with
`ModuleNotFoundError: No module named 'numpy'`: the integrated managed launcher
does not select the component's optional rendering requirements. Retry session
67198 is terminal (exit 1), with exact evidence in
`clara-source-network-render-check.xml`. No production import was changed to
hide this error. Under AGENTS.md's instruction to stop and report when resolving
a test import failure appears to require production changes, that follow-up
requires explicit scope resolution. The candidate is not runtime-qualified.

Subsequent read-only Platform inspection after unlock directly observed Vera
0.1.205, Clara 0.1.177 and Lucia 0.1.28 as **Published**. Evidence:
`/private/tmp/vera-remediation-01a07083/platform-publication-observation-after-unlock.json`.
This supersedes the earlier Clara Draft/Lucia Approved observations. It closes
the missing publication evidence, but does not make local Clara 0.1.173 source
identical to the published 0.1.177 package or prove complete release parity.
No publish control was operated by this task.

Integrated the already-authored registry correction from the clean marketplace
parity worktree commit `5db0bba5`: local Clara announcement is now 0.1.177,
matching the directly observed Published row. The notification test module
returns 16 passes and one failure. The former installed-cache mismatch is
closed; the remaining failure correctly exposes local Clara source 0.1.173
behind the published version. Its manifest was not artificially bumped to
hide this reconciliation gap. Exact JUnit evidence:
`/private/tmp/vera-remediation-01a07083/publication-registry-reconciliation.xml`.
This is a changed failure classification, not a fully green suite.

The user approved the six prepared synthetic model calls and authoring/two local
browser replays, requested an Italian questions-and-answers document, and
confirmed the Mac was unlocked. These permissions do not approve substantive
expected outcomes, developer transfer or publication.

- Six model calls completed normally; all six passed the production response
  validator. Luna/Astra selected identical links or abstentions on the three
  cases. See `PRODUCTION_MODEL_COMPARISON.md`; no superiority claim follows.
- Two actual connected-Chrome runs of shipped capability runtime/13 passed
  without recovery. Both downloaded and hashed the expected 196-byte ZIP,
  SHA-256 `da88429c87585cbfebd605aceb25a3d0fc06b08675dfe9abc7a9d229643313b5`.
  The pipeline verified both receipts and finalized `validated_local` at
  `/private/tmp/vera-remediation-01a07083/browser-capability-review/capability.validated.json`.
  This qualifies only the declared local synthetic form on this environment.
- `REVISIONE_PROFESSIONALE_IT.md` now supplies 16 Italian cases with proposed
  answers, correction questions and a blank review table. No professional
  outcome has yet been approved. ON FAQ conclusions were freshly checked;
  unresolved INPS source extraction is disclosed instead of inventing figures.
- Native LibreOffice opened the retained workbook after unlock, but crashed
  while navigating sheets. Its recovery dialog was captured; the two empty-data
  sheet visual gaps remain. Source workbook bytes remain unchanged.

## Latest released-source reconciliation checkpoint

Compared all release-changed Vera component/shared source paths against the
primary baseline `d4486cde`, local files and `origin/main` at `e320172e`.
The initial classification contained 83 release-identical files, 21 paths with
only incoming changes, and 50 overlapping paths (mostly candidate metadata and
privacy records). Integrated 17 inspected, locally unmodified source files:
Python 3.10-compatible date/enum handling in Bandi/XBRL and related helpers,
waterfall trace/role ordering, Polars distribution inputs and lazy scatter
imports. Updated affected component candidate versions and refreshed reviewed
privacy fingerprints. This comparison does not claim all unrelated Clara source
or the entire primary checkout matches origin/main.

A three-way review also found a missing dependency-check diagnostic change.
Both identical runtime managers now preserve failure details while retaining
this task's process locks and atomic generation publication. Existing Astra
skill simplifications and reviewed Codex/Cowork selection remain preserved;
overlapping worker logic was not replaced with a release copy. Historical
submission hash differences remain historical, not current release evidence.

Validation: 496 affected-workflow tests, 39 runtime tests and 433 package/version
gate tests pass. Mypy passes all 20 configured source files; the changed runtime
passes Black/Isort and has no medium/high Bandit findings. Both privacy registers,
all Codex/Cowork bundle and catalog checks, both Vera/Clara upload checks and
Vera's 18 packaged MCP startups pass. Six fresh packaged renders preserve chart
data; four PNGs are byte-identical to the earlier verified files. The two changed
histogram PNGs were inspected and have readable titles, period labels and axes.
No Python 3.10 interpreter was found in PATH; these results do not qualify that
runtime or Windows. The frozen full-suite record remains separate.

Exact source/package hashes, classification, merge review, tests and render
comparisons are under `/private/tmp/vera-remediation-01a07083/release-source-audit/`;
`final-checkpoint.json` binds the current candidate. The final lifecycle check
found three local branches, two remote branches, three worktrees and zero
stashes; none were created by this task. No merge, deployment or publication was performed.

The concurrent task “Fix Clara reporting QA findings” completed while these
checks ran. Its optional-currency acceptance CLI, `python-dateutil` dependency
mappings and histogram title-layout changes are present and preserved. Its
recorded Clara ZIP hash matches this checkpoint exactly. Its 48-capability
acceptance defaults to data-only; its four render cases are recorded separately.
The histogram PNG changes in this task's six-render comparison include that
owner's title-layout correction and must not be attributed solely to the
released Polars change. `concurrent-reporting-qa.json` binds the inspected source
and owner evidence. The owner also leaves the Clara publication discrepancy open.

## Latest cold-runtime and visual checkpoint

All 12 synthetic cold Cowork reporting cases now pass: the original two corrected
scatter cases plus ten remaining cases (513.56 seconds). They cover period trend,
boxplot, histogram, mix timeline, scatter and bubble with full dates and month-only
dates. Actual trend totals/month assignments and distribution statistics reconcile
with the input rows; both PNGs were inspected.

Visual inspection found a separate scatter/bubble title-clipping defect despite
successful rendering. The canonical export now gives the title 24 logical pixels
of top clearance and uses the chart's base text size. Twenty-five component tests
pass with one optional skip. Four fresh renders from the rebuilt package were
inspected after the fix: both chart types and both date formats show all three
original title lines. Chart, canonical and summary CSV values are unchanged in
all four before/after comparisons. Restricted Chrome-startup failures and HTML
fallbacks are retained separately; only the later actual PNGs support visual
acceptance. See `VISUAL_ACCEPTANCE.md` for evidence and limits.

After the title change, all Codex/Cowork package and catalog checks, both
Vera/Clara upload checks, both privacy registers and Vera's 18 packaged MCP
startups pass. Exact latest bytes are recorded in
`reporting-release-integration/title-final-package-identities.json` under the
external evidence directory. No deployment or publication was performed.

## Latest reporting integration checkpoint

Reconciled the released month parsing, resampling, currency and scatter fixes,
while preserving the local trend layout correction. Reporting checks: 305
passed with two title failures; the subsequent 19-test Distribution rerun closes
both. Shared reporting checks: 119 passed. Two real cold Cowork ZIP tests pass
and retain scatter/bubble PNGs and an HTML review page after correcting the test invocation to
select the already-published core and render requirements. No production import
or extracted-package patch was used to make those tests pass.

The combined package batch passed 414 tests and exposed one stale component
version failure. Fresh chart component candidate versions are Distribution
0.1.45, Period Comparison 0.1.68, Mix Contribution 0.1.102 and Scatter/Bubble
0.1.50. The version regression and three icon checks then pass. All Codex and
Cowork bundle parity checks, both Vera/Clara upload parity checks and both
privacy registers pass after rebuilding; Vera's 18 packaged MCP startups pass.
Cold render evidence precedes the final manifest-only version changes; the
before/after package hashes are retained separately. This is scoped validation
with targeted failure closure, not a newly all-green full-suite run.

Evidence: `/private/tmp/vera-remediation-01a07083/reporting-release-integration/`.
The repository has two local branches, two remote branches, two worktrees and
zero stashes; this task created none of them. Another task owns the temporary
marketplace-parity branch/worktree. No merge, deployment or publication here.

## Latest Cowork integration checkpoint

PR #540's native Cowork Haiku handoff is integrated with this task's reviewed
Codex model selection. An explicit runtime binds configuration, job identity
and checkpoints. Cowork rejects Codex selection/effort overrides; pending worker
responses retain outputs and exit 3 without claiming a completed audit. Native
Codex artifact recovery remains available for reviewed alternative models.
The packaged agent, instructions and dependency setup are preserved from the
released change. Host-reported records do not authenticate model identity or
establish accounting accuracy.

The exact updated Passive Invoice scripts pass **87 tests, one opt-in native
skip** in an isolated real-directory copy; current Mypy passes all 20 configured
source files and the component Bandit scan reports no medium/high findings.
A primary-checkout diagnostic encountered seven existing shared-assurance
bytecode files rejected by the exact native implementation contract. Those files
were not deleted and the production boundary was not relaxed. Evidence lives in
`/private/tmp/vera-remediation-01a07083/cowork-selection-integration/`.

The local candidate is **Vera 0.1.206**. Codex and Cowork builds succeeded,
including 18 packaged MCP startups. All 373 Codex package tests and all 48
Cowork package/privacy tests pass. Cowork parity checks pass for Vera, Clara
and Lucia and their catalog. These candidate bytes are not Published.
The full frozen suite finished with **6,172 passed, nine failed, eight skipped**
in 3,339.14 seconds. Seven failures were caused by the global test harness
clearing component imports; a test-only autouse fixture restores the component
path and module identities. The same harness now passes **89 tests, one skip**,
including those seven cases. Vera's stale announcement failure also passes on
the current source. One Clara installed-cache/publication discrepancy remains.
This is eight closed failures plus one open failure, not one all-green full run.

The integrated four-kernel correctness gate passes **256 tests, one native skip,
84.27% aggregate statement coverage**. The earlier minimal-copy attempts lacked
one test helper and two public HTML fixtures; those copies were supplied without
production changes. Mypy additionally passes the four connected Passive Invoice
core/CLI/Cowork files; the Cowork runner explicitly rejects Codex overrides
before writing a request. Package tests pass (373 Codex; 48 Cowork/privacy),
with subsequent formatting changes verified AST-equivalent and the final
runner signature covered by the 87-test component run and aggregate gate.
The frozen suite predates this integration; these are scoped delta results. Direct Platform observation verified Published Vera 0.1.205,
Clara 0.1.171 and Lucia 0.1.27. A later installed-cache observation of Clara
0.1.177 conflicts with its Draft status at that Platform observation; it is
retained as an unresolved release-state discrepancy, not evidence to advance
the announcement manifest.

## Coordination and baseline

Work is in the primary checkout, based on
`d4486cdec400b33a0a9b1c75b3ba7fb3e4705b91`, with pre-existing and concurrent dirty
changes. Task `01a070ac-a728-7e90-ba29-66554a4c5abb`, “migrate to GPT-6 Astra”,
simplified 17 skills and completed its separately authorized deployment through
PR #525. Its recorded final reports 9,168 passed, 16 conditional skips and 80.22%
coverage on its release tree. Those results do not certify this dirty remediation
tree. Its edits are preserved; reviewed test repairs were incorporated locally.
The shared Business Planning owner also completed a separate change; Vera and
Clara now use the same workflow and report semantics. No product-specific lens
was reintroduced here.

At the recorded PR #534 checkpoint, origin/main was `4aa125e8`, ahead of the primary checkout;
it includes Astra #525, Business Planning #534, waterfall and AML fixes. The
Business Planning owner reports a successful separately authorized production
restart, identical Vera/Clara server results and 94.68% planning coverage. These
are that owner's release results, not this snapshot's qualification.
No pull/reset was performed over concurrent local changes. Reconciliation with
that release revision and stable-tree qualification remain necessary before a
future release. Results below are scoped evidence, not an immutable release.

## Historical task-results matrix

This matrix predates subsequent qualification and package changes. Use the
current checkpoint above and dated evidence below for current results; retain
this matrix only as implementation history, not a final completion checklist.

| Task | Implemented or observed | Outstanding acceptance |
| --- | --- | --- |
| T01 | Required missing/nonfinite amounts and uncached numeric formulas reject; reviewed one-sided debit/credit blanks are explicit; conflicting latest balances stay unavailable; undefined ratios have readable reasons; all services remain in results. | Broader independent reporting evaluation belongs to T16. |
| T02 | Declared numeric conventions reject ambiguity and nonfinite values; original lexemes and physical source-row lineage are retained. | Independent accounting outcome evaluation in T15. |
| T03 | Capacity-aware stratified allocation fills available requested slots; deterministic seed and non-inferential method limitations are explicit. | Independent sampling-method qualification; no statistical confidence claim. |
| T04 | Actual PDF page text and coverage replace byte decoding; byte/text hashes differ; scanned/unreadable/partial sources remain explicit; URL path case is preserved. | Substantive source/legal acceptance in T17. |
| T05 | FatturaPA records have per-body metadata, amounts, body index and source hash; invalid sources have explicit disposition. Single-body consumers reject multiple bodies. | Broader independently reviewed extraction cases in T18. |
| T06 | Shared client-root OS lock covers apply/rollback; durable operation states and physical hash reconciliation recover interrupted local operations; Drive journals retain uncertain remote outcomes. | 68 archive tests pass, including 52 before/after file and journal boundary cases, competing apply/rollback and changed-destination preservation. These simulate process interruption; actual power-loss and Windows behavior remain unqualified. |
| T07 | Locked immutable runtime generations, atomic active pointer, resolved dependency receipt, bounded setup and failed-upgrade preservation; malformed/symlink pointers reject. Vera/Clara managers remain identical. | Actual Windows qualification; no Windows execution claimed. |
| T08 | Shared worker now rejects a symlink output leaf before path resolution, credential reads or launch; the regression failed before and passes after the fix in a clean guarded component copy. Read-only host diagnostic and retained candidate canary/normal-worker evidence distinguish prerequisite matches from qualification. Existing production pins remain enforced. The shared worker accepts a model/effort choice bound to the existing reviewed-decision receipt; 18 focused tests pass. | Journal–Bank graph/CLI/receipt propagation is implemented with 64 passing semantic/worker tests. Passive Invoice CLI, configuration, checkpoint and native-recovery propagation is implemented; the integrated Journal–Bank/Passive Invoice selection batch passes 130 tests with one explicit native-run skip. The subsequent complete Vera/Clara privacy refresh and package parity checks pass; the shared catalog failure has a passing targeted rerun. The OS independently confirms the synthetic CLI attachment read was denied. The separate hidden view_image canary remains inconclusive; configurable reviewed model/profile qualification remains unfinished. Initial synthetic model probes are recorded in MODEL_SELECTION_EVALUATION.md; they are not production-packet qualification. See HOST_QUALIFICATION.md. |
| T09 | Explicit 20-module type/security gate, managed-runtime and four-kernel correctness coverage gates, bounded MCP test harness and test-only component import restoration. Replay/tampering safeguards remain. | The frozen full suite is terminal: 6,172 passed, nine failed, eight skipped. Seven import-harness failures and Vera announcement drift have passing targeted reruns; Clara installed-cache/publication discrepancy remains open. Later Cowork, reporting and released-source deltas have separate scoped evidence. This is not one all-green final-tree run. |
| T10 | Generated source-derived workflow registry and CI drift check; semantic routing remains model-led; corrected archive host description preserves Astra simplification. | Ten user-approved synthetic current-policy routing cases completed: 10/10 expected routes after exact namespace normalization, 10/10 clarification decisions, no claimed operations. Manual response review retained; archive provisional-plan usefulness remains unproven. See ROUTING_EVALUATION.md. No qualified portable unified-CUA browser adapter or comparative model result yet. |
| T11 | Finalization requires hash-consistent model-data artifacts; Studio Archive provides a locally usable report builder, including in Lucia and Cowork, through one canonical vendored implementation; post-seal receipt retries use supplementary receipt directories and preserve original report identity/version. | No claim that local/server receipts independently prove provider transmission. |
| T12 | Lexical categories are candidates; reviewed kinds bind to extracted-text hashes; every source has a disposition; authorized successor decisions regenerate extraction/review/handoff together. Standalone parsing cannot invalidate an existing review package. New Client consumes both the sealed legacy and explicit candidate inventory contracts. | Wider independent multilingual/jurisdictional outcome cases. |
| T13 | Responsibility/duplication map, shared mutation-lock boundary and canonical model-report packaging overlay added. No pruning based only on static import absence. | The native successor workflow positively traces all five retained Open Item source modules; they must remain. Latest comparable measurements are in RESPONSIBILITY_MAP.md. Broader dynamic reachability and larger extractions remain unqualified; no package-size reduction claimed. |
| T14 | Incomplete scenarios no longer suppress complete scenarios; undefined calculations and unavailable claims stay explicit; existing Astra financial interpretation is preserved. | Independent economic interpretation and rendered-output acceptance. |
| T15 | Passive Invoice now combines reviewed Codex selection with Cowork pending/resume handling and exact runtime/response binding. Its current component suite passes 87 tests with one native skip; the global import-isolation batch passes 89 with one skip. Eight accounting review cases and their synthetic inputs are prepared in ACCOUNTING_REVIEW_CASES.md. | Independent expected matches, abstentions, reviewer labels and correction measurements remain incomplete. Fixture handoff records are not real Haiku acceptance. |
| T16 | Retained synthetic HTML/XLSX/DOCX outputs and numeric regressions exercise reporting paths. The selected DOCX passed a three-page visual inspection; an XLSX clipping fix is incorporated and passes all 150 Report Builder tests; five rendered sheets preserve all 83 original cells. See VISUAL_ACCEPTANCE.md. | Visual acceptance and independent claim-to-source evaluation remain incomplete. Browser opening of a retained local HTML file was blocked by tool policy; no alternate route was used. |
| T17 | Source extraction and mechanical contracts tested; eight concrete professional-review cases prepared in PROFESSIONAL_REVIEW_CASES.md; reviewer assignment requested. | Reviewer assignment and substantive case acceptance remain pending; inspected primary-source candidates are recorded in the review pack. |
| T18 | Synthetic XML, intake, engagement, archive-recovery and AML output evidence retained; existing Centrale Rischi/XBRL assets preserved. | Independent source-to-output acceptance across the full scope is incomplete. |
| T19 | Python/Node browser contracts tested; runtime 13 prevents the reproduced pre-rejection input/read leak. Documented unified-CUA Playwright interaction reached the synthetic form result and observed a download event. Function-specific privacy records reviewed where changed. | All 28 public-page contracts and the 77-test AML/privacy/page batch pass. Actual AML Chromium language/navigation/layout checks pass. Manual capability-fixture interaction is not a portable-runner/download qualification; changed-page/login/origin acceptance remains incomplete. |
| T20 | Vera 0.1.206 is rebuilt as Codex, ChatGPT-upload and Cowork candidate ZIPs. Vera parity and 18 packaged MCP startup checks pass; all Cowork family packages and catalog match their local source. Package tests: 373 Codex and 48 Cowork/privacy. Published rows observed: Vera 0.1.205, Clara 0.1.171, Lucia 0.1.27. | Clara installed cache later reached 0.1.177, which was Draft at the direct Platform observation; fresh verification awaits Mac access. Current primary Clara/Lucia source remains 0.1.173/0.1.26. No deployment or publication by this task. |

## Validation evidence

Bulky logs, XML summaries and retained synthetic outputs live at
`/private/tmp/vera-remediation-01a07083/`. Temporary evidence can be cleaned by the
OS; this record states the conclusions without requiring those files.

- The complete snapshot traversal finished: **5,409 passed, 229 failed, seven
  skipped**, 5,645 collected nodes. Of the failures, 225 arose from the snapshot's
  initially missing Git-worktree environment or its consequences; the remaining
  four concerned stale privacy, published-version and AML label expectations.
  `full-plugin-failure-closure.json` maps every failed node to a passing targeted
  rerun. No unresolved failure from that traversal remains, but it is not a
  single all-green run on the latest source.
- Four of the seven skips subsequently passed in approved, isolated host renderer
  tests (`host-renderer-qualification.xml`). They created their own synthetic
  documents; the earlier policy-blocked retained HTML was not reopened. The
  three remaining skips require unavailable external historical HTML/PPTX holdouts.
- Latest four-kernel correctness gate: **227 passed, one opt-in native-Luna skip**,
  **83.64% statement coverage** against the 80% CI gate (`current-correctness.xml`).
  The earlier run without Node on PATH is superseded. Mypy passes all 20 configured
  source modules. Scoped Bandit reports no medium/high findings; this is not a
  whole-repository security qualification.
- Archive component: **68 passed** (`archive-recovery-current.xml`), including
  **52** before/after interruption cases across both file copies, flushes, fsyncs,
  unlinks and all six apply/four rollback journal writes. Recovery is repeatable;
  an incomplete destination remains preserved for explicit review. Competing
  public apply/rollback calls and changed-destination preservation pass. These
  tests use injected exceptions and threads, not hard power loss or Windows.
- Package integration: 432 passed and two stale announcement assertions; all 17
  announcement tests pass after published-version reconciliation. Website journeys:
  **181 passed**, including the already-released AML label correction.
- Report Builder: **150 passed**; five rendered XLSX sheets preserve all 83
  original cell values/types, and three DOCX pages were visually inspected.
  Four synthetic headless renderer checks also pass. Independent report quality
  acceptance remains separate from layout and structural checks.
- Browser/discovery Node contracts: **46 passed** on runtime 13. The browser Python
  batch had 173 passes and one stale version expectation; all 69 tests in that
  affected file then passed. Two actual capability replays still require the
  requested explicit authoring approval.
- Managed runtime: 28 passed, 82.62% module statement coverage. Three packaged
  Cowork report-generation/finalization round trips and 18 MCP startup/tool-list
  checks pass. The approved Astra monthly-P&L handoff passes all 30 tests.
- Other focused batches remain supporting, overlapping evidence: component 282,
  planning/privacy 84, reconciliation/reporting 153, variance 67, intake/delegation
  61, review workbench 73 and final gaps 41 passes. Never sum overlapping batches.

The validation copy contains its own isolated unborn Git repository, created
during the full run after the missing-worktree error was diagnosed. That timing
is recorded in its manifest; source files were not changed mid-run. Future
snapshot preparation initializes Git before testing. Canonical Attribute
Reporting tests now create their own temporary forbidden-workspace fixtures.
No production guard was weakened and concurrent bytecode caches were untouched.
The latest source changes are qualified by the stated targeted reruns, not by
claiming that the older snapshot contains them.

## Subsequent native-boundary correction and concurrent work

The generic shared worker checked `is_symlink()` only after resolving the output
path, so a symlink to an existing directory reached authentication-boundary
preflight. The new regression failed at that preflight in a clean copied
component; canonical code now checks the unresolved leaf first. The identical
regression passes and the target remains empty. No host pins, model selection,
read allowances or canary requirements changed. Journal–Bank is now 0.1.46;
its privacy record and the dependent Passive Invoice binding were reviewed and
refreshed. Evidence: `native-output-symlink-clean-before.log` and
`native-output-symlink-after.xml` under the temporary evidence root.

The first primary-checkout attempt encountered another unowned bytecode cache;
that was an import guard failure, not the reproduced symlink defect. No guard
was weakened or primary cache removed. The focused copy retained all ordinary
component/shared-assurance files and omitted only bytecode, then ran the public
entrypoint test extracted unchanged from canonical test source.

The full-suite snapshot remains unchanged and predates this correction. Current
Bandi 0.3.5 edits differ from that snapshot and belong to active task
`01a071e2-9f73-7883-8ae3-684036c3ba16`, “Fix CR-38 CR-39 CR-40”. Its stale privacy
record was not refreshed by this task to approve unreviewed concurrent work.
A subsequent complete Vera register validation passes after the owner’s review.
Separate runtime
and reporting-registry tasks are active too. Rebuild/reconciliation must account
for these changes without claiming the old snapshot qualifies them.

## Evaluation population correction

The existing Passive Invoice evaluator previously overwrote duplicate result IDs
in a dictionary and counted duplicate labels repeatedly. Both public regressions
failed before the change. The evaluator now rejects duplicate/missing identities
and unsupported final states before producing a report; metrics explicitly expose
result, labelled, unlabelled and ambiguous counts and label coverage. Rates still
use the labelled population. Synthetic cases verify arithmetic, not independent
professional label quality. Component version 0.1.3 and its privacy binding are
current. Evidence: `evaluation-duplicates-before.xml`, `evaluation-full-passive.xml`
(**58 passed, one opt-in real-Luna skip**), and `evaluation-types.log` (20 configured
source files pass). The two newly changed kernels have zero medium/high Bandit
findings (`native-evaluation-security.log`). The original full-suite snapshot
predates these changes and is now terminal; see the scoped closure evidence above.

## Validation environment and released-version reconciliation

At about one third of the full run, Clara private-pilot tests rejected the copied
snapshot because `repository_root` must be a Git worktree. A focused public test
reproduced that exact error. The snapshot now has its own isolated, unborn Git
repository (no remote and no shared administration with the primary checkout).
The source files were not altered; this environment change happened during the
run and is recorded in `validation-tree-manifest.json`. The snapshot preparation
script now creates that environment before testing on future runs.

The affected-file rerun completed **407 passes and two failures**
(`snapshot-git-environment-recheck.xml`). One failure was a synthetic receipt
left behind by the earlier invalid environment; the production guard correctly
rejected it, and the canonical self-contained fixture passes. The other was
stale Clara Business Planning/root fingerprints, now reviewed against exact
released source bytes and refreshed. The full run retains its earlier failures;
no single all-green invocation is claimed.

PR #536 (`8609f720`) already released one product version across Codex, ChatGPT
upload and Cowork. Its six-line builder change and version-parity tests are now
incorporated while preserving this task’s other builder changes. Source Claude
manifests are metadata templates without independent versions. The released
runtime manifest-name lookup was already present, and the immutable-generation
work remains. At that checkpoint local versions were Vera **0.1.201**, Clara **0.1.172**
and Lucia **0.1.26** (superseded below); Business Planning is reconciled to **0.2.5** (its inspected
implementation bytes match PR #534). Public published-version metadata was subsequently reconciled to Vera **0.1.200**
and Clara **0.1.171**, after inspecting the separate release owner’s publication
evidence. These are distinct from the local development versions.

All nine rebuilt ZIPs contain the canonical product version; hashes and manifest
entry paths are retained in `unified-version-package-identities.json`. Three
builder regressions pass. Both complete privacy registers validate, and all
13 Clara privacy tests pass. Review covered the runtime-generation changes,
metadata/registry differences, and five appended Bandi privacy-text paragraphs
in a shared JavaScript file; that last change leaves the governed Research Video
voice implementation unchanged. The package test batch finished with 432 passes and two stale announcement
assertions in `unified-version-package-tests.xml` / `.log`; the subsequent
`released-announcements-current.xml` has all 17 tests passing. Clara’s three
variants were rebuilt and source-checked again after the announcement update. The original full-suite snapshot
predates these source and package reconciliations.

## PR #535 source reconciliation and current candidate

The separate Clara release task completed PR #535 (`54f75ff5`). Seven files in
this checkout still exactly matched that commit's parent. Their released changes
are now incorporated: strict numeric-string arithmetic, correct last-point trend
labels, three Python 3.10-compatible UTC imports, declared renderer `psutil`, and
the corresponding period-comparison tests. No locally modified file was replaced.
The release gate, registry and packaging projection were already present.

The focused reporting/runtime batch passes **83 tests with one renderer skip**
(`released-reporting-runtime-recheck.xml`). Local development versions are now
**Vera 0.1.202, Clara 0.1.173, Lucia 0.1.26**, distinct from the separate release.
All nine rebuilt ZIPs pass source drift checks; 18 packaged MCP servers initialize
and list tools. The changed privacy bindings were reviewed: UTC representation,
local numeric arithmetic/rendering and declared dependency changes introduce no
new model transmission path. Both privacy registers validate.

The latest package/notification batch is **389 passed, one failed**
(`reconciled-package-check.xml`). The sole failure compared public Vera 0.1.200
with installed cache 0.1.201. The already-open Platform tab showed stale Draft
state; a freshly loaded page instead confirms **Vera 0.1.201 Published**, Clara
**0.1.171 Published** and Lucia **0.1.25 Published**. The local Vera announcement
now matches that fresh observation. All **17 announcement tests pass**
(`vera-0201-announcement.xml`); no test or guard was weakened.

The exact pre-announcement-update Clara 0.1.173 Cowork ZIP passed all **21** fresh
packaged-runtime steps on macOS/Python 3.12, including declared dependencies,
intake, semantics, rendering and negative missing-role rejection. The initial
restricted-network attempt failed and is retained separately. Approved host
execution evidence lives in `clara-0173-packaged-runtime-host`; its exact ZIP is
retained as `accepted-script-candidate.zip` with SHA-256
`eb21f2538c472344fbd37474705d7761e6bb91ba137306172c61d840dad57fe1`.
This is a local diagnostic, not Linux or actual Cowork agent acceptance.

Visual inspection of that run revealed a clipped trend title and an excessive
900-pixel canvas despite the successful script result. The single-panel
`year_over_year_line.png` export now uses a 540-pixel minimum height, keeps title
and month labels inside the canvas, and gives the title the normal chart font
size. Values, series marks and zero-anchored scale are unchanged. The corrected
PNG was rendered in the declared managed runtime and visually inspected;
**32 period-comparison tests pass with one conditional renderer skip**.
Component version is **0.1.66**. Evidence: `period-trend-layout.xml` and
`trend-after/year_over_year_line.png`. The source gallery review helper could
not run because its generated gallery manifest is absent; the exact runtime PNG
and cached direct-line reference were inspected instead.

The final announcement/privacy metadata and trend-layout correction postdate
the exact ZIP used by the 21-step script run. All six Vera/Clara variants have been
rebuilt and source-checked after these edits; the final-ZIP runtime check
passed all **21 steps** at `clara-0173-final-runtime`, with its ZIP hash verified
against the current artifact (`c5a186d5d13b3b2bf3f582bf30c7e17988470432877906e6aa8d57da1f5fde21`); the earlier whole-ZIP receipt remains separate. The final packaged PNG is
byte-identical to the visually inspected correction. Linux and actual Cowork
agent acceptance are still not established by this macOS/Python 3.12 check. Nothing was deployed or published
by this task.

## Repository inventory

Latest observed counts: two local branches, two remote branches (excluding
origin/HEAD), two registered worktrees and zero stashes. The additional
branch/worktree belongs to the separate Cowork runtime compatibility task. This task created no branch or worktree in the primary repository. Its temporary
validation copy has separate, unborn Git metadata solely to reproduce the
repository boundary required by tests; it is not a linked primary worktree. The primary checkout retains authorized remediation and
other owners' changes; no cleanup removed them.

## Completion boundary

Do not mark all twenty tasks complete from these checks. Actual host isolation,
portable browser execution, independent professional outcomes and release-state
reconciliation remain separate evidence requirements. Do not weaken guards,
publish unverified versions, or remove useful tests to make this record green.

## Import-isolation overhead correction

The common test hooks now resolve the repository/plugin root once per isolation
pass. Module paths still resolve individually; no module classification is
cached across tests. Two new public-hook regressions cover symlinks resolving
inside and outside the plugin tree. A combined run with run-receipt and icon
tests passed all ten tests (`import-hooks-regression.xml`).

A small three-test profile recorded 8,589 path-resolution calls before and 6,110
after; all three tests passed both times. The observed test duration changed
from 0.82 to 0.73 seconds, but concurrent workload and the small sample prevent
a general speedup claim. Profiles and logs are `import-hooks-{before,after}.*`
in the external evidence directory. This is test-only work; production import
and tamper checks are unchanged. The ongoing full-suite snapshot predates this
optimization and must not be cited as validation of it.

## Latest frozen integration run and reviewed worker selection

The second frozen snapshot traversal completed with **6,119 passed, eight
failed, eight skipped** in 3,464.90 seconds (`current-full-plugin-suite.xml`).
Its 5,248-file snapshot predates the reviewed worker-selection implementation
and the import-hook optimization. All eight failures have been inspected:
three pilot-intake tests assumed an existing checkout `out/` directory; three
checks detected stale generated packages/catalogs; and two detected stale
Vera/Clara privacy fingerprints. This is not an all-green current-source run.
The intake tests now create a minimal temporary Git repository, tracked source
and ignored output directory. All **65** intake tests pass
(`pilot-intake-isolated.xml`); no production validation was changed.
Package and privacy failures still require reviewed refresh and reruns.

Reviewed model selection now propagates through both Journal–Bank and Passive
Invoice. Passive Invoice binds the selected model, effort and complete review
record into job fingerprints, checkpoints, native receipts and recovery checks;
a changed or rehashed inconsistent receipt cannot resume an old job. Selection
is an authorized engagement input to the audit and synthetic-evaluation CLIs.
The shared worker returns the normalized selection record for downstream
comparison. The integrated selection batch passes **130 tests, one explicit
native-run skip** (`worker-selection-integrated.xml`), and all 20 configured
source files pass Mypy (`worker-selection-types.log`). Tests use mocked native
launches; no alternative model was actually qualified or selected for use.

Journal–Bank and Passive Invoice privacy records were reviewed and refreshed.
The Passive Invoice public final model-data disclosure now explains the default
and reviewed alternative in all five languages. Complete-register checks still
report six other stale Vera entries and the Clara Business Planning entry;
these require source review before refreshing. Generated bundles from the
previous checkpoint predate these changes and must be rebuilt before any claim
of source/package parity. No production host pins or containment requirements
were relaxed.

### Privacy reconciliation after the frozen suite

Both complete Vera and Clara privacy registers now validate as current. The
three dependency-gate changes and two version-bound service changes preserve
their previously reviewed data paths. Shared Business Planning presentation
changes were reviewed separately: both records now describe source-bound tables,
action owners/timing/criteria, reference URLs, PDF appendix visibility and draft
export. Clara now fingerprints `planning_presentation.py` explicitly. This is
engineering boundary review, not professional or privacy-law certification.

The privacy/presentation/public-page batch produced **76 passes and seven
failures** (`reviewed-privacy-presentation.xml`). The remaining failures concern
website architecture: AML breadcrumb mapping, shared model-data component and
run-report note, shared typography, Vera jurisdiction/section expectations and
long model-data paragraph structure. They remain open; refreshing privacy
fingerprints does not close them. Vera and Clara packages were rebuilt from
canonical source, with 18 packaged MCP servers initialized during the build.
Source-parity and package-integrity checks are underway; completion is not yet
claimed for this build checkpoint.

### Package and frozen-failure closure checkpoint

Vera 0.1.204 and Clara 0.1.173 Codex/Claude/Cowork generated artifacts pass
source-parity checks. The package/notification batch reports **433 passed and
one stale shared-catalog failure** (`selection-package-tests.xml`). Selecting
only two products in the existing builder does not regenerate the complete
marketplace catalog. Calling its existing `build_catalog` with all configured
products regenerated that file without rebuilding Lucia or changing production
logic. The failed public test now passes (`selection-catalog-rerun-node.xml`).
An intermediate rerun omitted bundled Node from PATH and failed explicitly for
that prerequisite; its separate log is retained, and no source was changed to
bypass the prerequisite.

Every one of the eight failures from the frozen 6,119-pass traversal now maps
to a passing targeted rerun in `current-full-plugin-failure-closure.json`.
This does not make that traversal an all-green current-source run. The seven
website-contract failures remain open, as do independent professional review,
unsupported-host qualification and the separately requested browser/production
model comparisons. No deployment or publication was performed by this task.

The latest read-only Git inventory reports **one local branch, one remote
branch, one registered worktree, zero stashes**, at HEAD `d4486cdec400b33a0a9b1c75b3ba7fb3e4705b91`.
No branch or worktree cleanup was performed by this task at this checkpoint;
other tasks own their lifecycle changes.

## AML public-page integration and website failure closure

The AML explanation page loaded no shared page JavaScript. It now uses the
existing function model-data component and navigation styles; the shared
navigation registry maps AML to Vera's client-work area. A model-data body
container lets the existing component append its localized run-report link.
The five-language workflow explanation was preserved. No new data transmission
or professional claim was introduced.

The public-page architecture suite now passes all **28** tests
(`page-contract-current.xml`). In addition to the functional integration gap,
its old assertions expected seven Italian-only links, nineteen standalone
pages, no contributor section, three Bandi paragraphs and inline Business
Planning translations. They now recognize the added AML link/page, retained
contributor section, four source-boundary Bandi paragraphs and shared Business
Planning translation object. No substantive disclosure was shortened to satisfy
a layout assertion. All seven previously recorded website failures are closed
by this run; this does not qualify the still-pending browser capability replays.

An actual local Chromium check passed six language transitions (IT/EN/FR/DE/ES
and back to IT), exactly one breadcrumb and run-report note, localized links,
final-section ordering, absence of page script errors and absence of mobile
horizontal overflow. Desktop and mobile full-page captures were inspected.
The runner intercepted only these repository page assets and blocked every
external request, including web fonts; the captures therefore do not establish
production font loading. The initial sandbox launch failed with macOS Mach-port
permission denial. A reviewed host execution passed without changing the page
or browser restrictions. Evidence, exact source hashes and captures are retained
under `aml-page-browser/` in the temporary evidence directory.

The subsequent AML component + Vera privacy + public-page integration batch
passes **77 tests** (`aml-page-integration.xml`). This verifies the page change
alongside the AML component and the current Vera privacy register. It does not
provide independent professional AML acceptance. The new edits are public static
files and tests, so the previously rebuilt plugin source payloads are unchanged.

## Fresh complete-suite qualification in progress

A new clean validation tree contains **5,251 files**, including the current
worker-selection code, isolated intake fixtures, import-hook optimization and
AML page integration. Every copied file was rechecked against source; none
changed during copying. Only disposable Python bytecode was excluded from plugin
roots; unowned implementation/configuration files remain subject to guards.
The snapshot has independent unborn Git metadata and uses the existing declared
virtual environment. It does not share the primary checkout's Git administration.

Manifest: `validation-final-tree-manifest.json`, SHA-256
`a4078ceaa780fb2f7a4494413cc240c886885c5809eaefcfd0a6c9ca42823a99`.
The full invocation covers `tests/plugins`, `plugins`, browser capabilities,
public function-page architecture and import-isolation tests. The CI four-kernel
correctness/coverage gate, 20-module Mypy gate and scoped Bandit gate run against
the same snapshot. Their status is in `final-validation-status.json` under the
temporary evidence root. These checks remain in progress; this paragraph does
not assert successful completion or platform/professional qualification.

The fresh correctness gate is terminal: **235 passed, one explicit native-run
skip, 83.74% aggregate statement coverage** across its four configured kernels
(`final-correctness.xml` and `.log`). Mypy passes all 20 configured modules
(`final-types.log`). Bandit scans 32,460 lines and reports zero medium/high
findings and four low findings (`final-security.log`). These are scoped gates,
not whole-repository coverage/security claims. The complete plugin suite remains
running on the same unchanged validation snapshot.

### Formatting and canary follow-up during full-suite execution

Black and Isort pass all 20 source modules configured in the plugin quality
gate (`configured-source-format-check.json`). The recent test files and worker
entry points also pass their checks. The current checkout differs from the
frozen suite by one Black adjustment to the public-page test (AST equality
verified), alphabetical ordering of the independent `copy`/`csv` imports in
Passive Invoice (exact replacement equality verified), and the consequent
reviewed privacy fingerprint. These differences are recorded in
`page-test-format-equivalence.json` and `passive-import-format-equivalence.json`.
No frozen validation source was edited. The affected Vera package requires
regeneration after the formatting-only source change.

The hidden-image canary was re-examined: both `code_mode` and `code_mode_host`
are deliberately disabled in the production worker feature list. Enabling them
would change the configuration under test, so this task did not turn them on
to manufacture an image-tool result. The existing unavailable-tool response is
still not evidence of an OS-level denied read. Candidate qualification remains
separate from the successful ordinary sanitized worker and OS-confirmed CLI
image-attachment denial.

## Concrete accounting review preparation

`ACCOUNTING_REVIEW_CASES.md` now defines eight synthetic T15 cases covering
partial payments, reversals, duplicate evidence, grouped payments, cutoff timing,
currency conflict, missing support and changed source evidence. The local CSV
pack includes exact file hashes and a blank reviewer worksheet. A03's two source
files are byte-identical; A08's original/replacement differ. Those identities
were checked. Expectations remain explicitly proposed and unreviewed.

The CSVs are reviewer evidence, not approved workflow mappings. No pipeline run,
professional acceptance, false-match rate or reviewer-correction rate is claimed.
The pack specifies which source documents and mapping decisions remain necessary
for each accounting workflow, and how to preserve denominators, abstentions and
unlabelled cases during subsequent evaluation. This does not substitute for the
independent review required by T15 or the full-scope execution requirement.

Vera was rebuilt after the import ordering and fingerprint update. Its Codex
and Claude/Cowork package checks pass (`final-format-codex-check.log`,
`final-format-claude-check.log`); 18 packaged MCP servers initialize and list
tools. The three ZIP hashes are retained in
`final-format-vera-package-hashes.json`. No version announcement, deployment,
Marketplace submission or publication was performed. The complete frozen
plugin-suite process remains active and has not been restarted.

The retained Centrale Rischi workbook has now received an all-sheet visual
review: 18 sheets, 25 initial views and four targeted fresh-process rerenders.
Sixteen sheets render legibly; two empty-data sheets still lose labels despite
correct values in both saved cells and the renderer's public value API. The
renderer failure and exact workbook hash are recorded in VISUAL_ACCEPTANCE.md
and `workbook-review/cr-all/review-result.json`. No workbook or generator change
was made, and full visual acceptance remains incomplete for these two sheets.

## New released Cowork work requiring reconciliation

The release owner now reports Vera 0.1.205 and Lucia 0.1.27 published/deployed.
`origin/main` is 45e92078 (PR544 announcement), and includes PR540 b2ddb316:
native Cowork/Haiku Passive Invoice handoff, pending-review status/CLI exit,
Python 3.10 timezone compatibility and builder projections. This task has not
independently confirmed the authenticated Platform Published rows: automatic
approval review denied opening that origin, and a narrowly scoped read-only
permission request is pending. The existing live-registry/hash evidence was
inspected in the owner's task; publication remains attributed to that task.

PR540 overlaps with this task's later reviewed-model selection in job identity,
result/effort checks and recovery. Three-way comparisons are prepared outside
canonical source in `cowork-selection-integration/`; audit_core has ten conflict
regions, the two CLIs have three combined, tests one, while dependency checking
and the builder merge without textual conflicts. No tentative merge was applied
to production source. These source changes need coherent integration and focused
qualification before a final release candidate can be claimed. The running
frozen suite remains useful evidence for its baseline but predates this newly
identified integration requirement.

### Direct Platform observation after read-only approval

OpenAI Platform Plugins was inspected through an owned Chrome tab after explicit read-only approval. Published rows: Vera 0.1.205, Clara 0.1.171, Lucia 0.1.27. Clara 0.1.177 was Draft and Lucia 0.1.28 Approved. No publication controls were used. Local announced Vera version updated to 0.1.205 while preserving Clara 0.1.171; this does not deploy the registry or qualify local remediation packages. Evidence: `/private/tmp/vera-remediation-01a07083/platform-publication-observation.json`.

### Subsequent reporting release reconciliation

Observed origin/main advanced to `e320172e` (PR #545). Reconciled the released
monthly-date parser, first-of-month resampling, explicit/unstated currency
handling, scatter rendering hook, and single-period distribution title against
this task's local changes. The already-tested Period Comparison trend canvas and
margin correction remains present. Changes cover the shared chart helper and
Period Comparison, Mix Contribution, Distribution and Scatter/Bubble components.
This is targeted source reconciliation, not a claim that every intervening
Clara release has been integrated or published here.

Validation: the five-component reporting batch passed 305 tests and exposed two
single-period title failures; after including the released title fix, all 19
Distribution tests passed. The separate chart harness, period labels, reporting
titles, Variance and Set Overlap batch passed 119 tests. Both privacy registers
are current; Clara's update-service fingerprint was refreshed after confirming
that only the already-verified Vera announcement version changed among its
governed files. Candidate Vera/Clara bundles were rebuilt from canonical source.
The two cold ZIP scatter/bubble runs first failed at sandbox DNS resolution of
PyPI. The network-enabled retry exposed a test invocation error: it selected
only Reporting Engine's core requirements, then requested rendering. Its
published `requirements-render.txt` already declares NumPy and the other render
libraries. The test now selects both published requirement files; no production
imports, declarations or extracted ZIP contents were patched. Both corrected
cold tests pass (2 tests, 98.20 seconds), producing actual scatter/bubble PNGs and an HTML review page
with month-only dates and no invented currency. Outputs and SHA-256 records are
retained under `reporting-release-integration/cold-render-artifacts/`.

Both upload ZIPs were rebuilt after the final title fix. Vera and Clara Codex,
Cowork and upload parity checks pass; Vera's 18 packaged MCP startup checks
pass. These results qualify the tested local candidate bytes, not publication
or independent professional acceptance.
Evidence: `/private/tmp/vera-remediation-01a07083/reporting-release-integration/`.

### Partial bank payment correction — 2026-09-05

Approved A01 now produces `partially_paid`, with original EUR 1220.00,
allocated EUR 488.00 and residual EUR 732.00. The shared allocation ledger
retains the invoice face amount, consumes the payment once and explicitly
records the EUR 732 target residual (`balanced: false`, correctly reflecting
an outstanding obligation). Final assurance checks the retained partial ledger.
The Word report and operational Excel expose the allocation and residual with
record references; the original amount is preserved.

Automatic partial allocation requires one exclusive exact document reference,
positive partial amount, dated evidence on/before the cutoff and compatible
accounting perimeter. Ambiguous references, multiple candidate payments,
reused evidence, currency mismatches and post-cutoff payments do not qualify.
This is normalized-input developer verification, not full source-intake or
professional acceptance. Evidence and generated reports are retained under
`/private/tmp/vera-remediation-01a07083/partial-payment-corrected`; the result
summary is the adjacent `partial-payment-corrected-result.json`.

85 focused helper/workflow/report tests passed before the last Word-report
addition; all nine public workflow tests passed again after that addition.
The broader suite exposed 14 obsolete cache-rejection expectations: the current
bootstrap ignores cache contents and loads inspected source directly. Tests now
require successful source startup, unchanged implementation receipts and no
execution of the malicious bytecode marker. No production cache behavior was
weakened to satisfy those tests. A transition-history failure during concurrent
source editing reported stale implementation receipts; final validation runs
against the finished implementation. See `partial-payment-final-suite.xml`.

Open-item privacy fingerprint was refreshed after review. Five other workstreams
remain stale (check-entries, concordato-plan-review, journal-bank-reconciliation,
journal-sampling, report-builder). They are not cleared by this correction.
No deployment or publication was performed. Other tasks own the existing
branches/worktrees; observed counts are 4 local branches, 3 remote branches
(excluding origin/HEAD), 4 worktrees and 0 stashes.

Final Open-item module validation: **259 passed, zero failures/errors/skips**
(`partial-payment-final-suite.xml`, 70.120 seconds). Vera Codex ZIP rebuilt and
`--check` passed against current source. This closes the partial-payment defect;
it does not close the remaining T01–T20 acceptance and release work.

### Privacy reconciliation verification — 2026-09-05

Reviewed and refreshed the five remaining stale workflow records. The shared
cache-repair implementations are byte-identical across those components; their
SHA-256 is `63b7f29c714a443542639911e1a86ad63e33fac63e97eac72bcfa93958d542b1`.
The bytecode-boundary suite passes 54 tests; Vera/Cowork privacy suites pass 34,
with zero failures or skips. JUnit records are `privacy-bytecode-validation.xml`
and `privacy-register-final.xml` in the retained temporary evidence directory.
The complete Vera privacy validator passes. Rebuilt Codex and Cowork bundles
from canonical source and verified both drift checks; Cowork initializes and
lists tools for all 18 MCP servers. These records supersede the five-stale-
workstream note above and do not establish complete T15–T20 acceptance.

### A04 source-to-report correction — 6 September 2026

A real grouped-payment PDF run exposed duplicated source capacity: two invoice
references produced two full-amount bank rows. Corrected to one physical bank
record with multiple document keys. With the approved case's explicit exact-
allocation assumptions, actual reports contain two closed invoices (1220, 610)
and one balanced ledger consuming one source of 1830. Final Codex review remains
pending. See SOURCE_INTAKE_FINDING.md for mapping assumptions and retained runs.
The operational workbook now distinguishes the per-invoice amount difference
from the group difference. Both advisory and exact-closure output paths pass.

The completed module suite passed 267 tests with no failures/errors/skips
(`grouped-final-suite.xml`, 64.874 seconds). All 11 workflow tests passed again
after restricting localized field aliases (`grouped-workflow-final.xml`).

Current integration caveat: subsequent concurrent changes made privacy records
stale again for Check Entries, Concordato, Journal–Bank, Journal Sampling,
Report Builder, plugin-update-check and run-receipt-stamping. Open Item was
reviewed/refreshed for this correction; those other fingerprints were not
blindly refreshed. The earlier complete-register pass describes its recorded
snapshot and must not be treated as the latest tree's privacy status.

### A05 source-to-report correction — 6 September 2026

Actual imported PDFs now produce an open EUR 1000 invoice at the cutoff and a
separate 2 October subsequent payment. Corrected the operational "paid" label
so mere evidence presence cannot override the reconciliation result. Preserved
exact same-perimeter arithmetic for the rendered amount difference. All 15
focused report/workflow tests pass. The retained fresh run has only final Codex
review pending. See SOURCE_INTAKE_FINDING.md for exact evidence and limits.
Seven unrelated privacy records remain stale; this correction refreshed only
Open Item. No deployment, merge or publication has been performed.

## Managed review and packaging checkpoint — 6 September 2026

The Open Item managed predecessor-context fix passes the complete 271-test
module suite. A07's real synthetic PDF source run progresses through persisted
Codex decisions and normal successor regeneration to `final_ready`, retaining
two unresolved invoices and no allocation. An isolated CLI verifies the correct
external checkpoint and rejects missing/wrong checkpoints. The source-intake
finding contains the artifact locations and remaining professional-review limits.

Both Vera package rebuild/drift checks pass, including initialization/tool
listing for all 18 packaged MCP servers. The package/update suite initially
reported 388 passes and two extracted Clara renderer failures caused by sandbox
DNS denial during declared managed-dependency installation. The exact two tests
pass on retry with approved host network access. Evidence is
`snapshot-package-tests.xml` and `snapshot-package-network-retry.xml` in the
thread evidence directory. This is a successful scoped retry, not a claim that
the entire repository suite was rerun. Seven other privacy records remain stale.

A03 now has an actual source run demonstrating a single EUR 1220 invoice after
content-addressed deduplication. Its remaining product gap is durable duplicate
occurrence lineage reaching the normal report. The externally retained driver
observation does not satisfy that requirement. T01–T20 remain active.

Git inventory observed at this checkpoint: two local branches, two remote
branches excluding origin/HEAD, two registered worktrees, zero stashes. This
task created none of them and preserved the other task's parity worktree.
No deployment, merge or publication was performed.

## A03 final lineage/package checkpoint — 6 September 2026

A03's normal source-to-reviewed-final run now shows both imported names in Word
and Excel and counts one EUR 1220 invoice. Both Vera package drift checks pass;
Clara and Lucia packages were also rebuilt because they consume the changed
shared context/Studio Archive code. The package/update/assurance-packaging suite
reports 391 passes and two extracted Clara renderer failures from sandbox DNS
restrictions. The same two tests pass with approved network access for declared
managed dependencies (`duplicate-package-network-retry.xml`). No full-repository
or complete privacy-register pass is claimed.

The next A02 source case is in progress. Initial synthetic bank text emitted no
qualified rows; the output was correctly withheld. The invoice and reversal
both extract. Diagnostics are retained in
`/private/tmp/vera-remediation-01a07083/source-acceptance-a02/extraction-diagnosis.json`.
A separate grouped-money formatting attempt passes intake and reaches report
sealing, where `Importo evidenza` value 1220 has no accepted source formula for
the two invoice/reversal owner rows. The exact failure is in
`/private/tmp/vera-remediation-01a07083/a02-qualified-format.log`. Continue by
examining that evidence lineage and the reversal/payment treatment; this is not
a demonstrated A02 economic pass or a reason to bypass source qualification.

Observed Git inventory remains two local branches, two remote branches excluding
origin/HEAD, two worktrees and zero stashes. This task created no branch or
worktree; unrelated work was preserved. Nothing was deployed or published.

## A02 final package checkpoint — 6 September 2026

The complete package/update/assurance-packaging batch passes all 393 tests with
approved host network access for declared managed dependencies
(`reversal-package-suite.xml`). Both Vera Codex/Cowork package drift checks pass;
all 18 MCP servers initialize and list tools. Combined with 276 Open Item tests
and the actual A02 source/review/successor run, this verifies the scoped reversal
fix. It does not establish a full-repository gate or resolve the pending privacy
fingerprints in other shared-contract consumers. A08 source-change acceptance
and the remaining T01–T20 requirements are still active.

Git counts remain two local branches, two remote branches excluding origin/HEAD,
two registered worktrees and zero stashes. This task created no branch/worktree
and preserved the unrelated parity task. No deployment, merge or publication.

## A08 package checkpoint — 6 September 2026

The package/update/assurance-packaging suite passes 393 tests in one batch
(`source-review-package-suite.xml`) with approved host network access for declared
managed dependencies. Both Vera package drift checks pass, including successful
initialization and tool listing for all 18 MCP servers. The source-review
invalidation fix therefore has module, actual source/review/successor, isolated
replay and package evidence. Other privacy fingerprints remain pending; the
whole T01–T20 goal is not complete.

Git inventory is unchanged: two local branches, two remote branches excluding
origin/HEAD, two registered worktrees, zero stashes. This task created none and
preserved unrelated work. No deployment, merge or publication.

## A01 residual correction verification, 6 September

Fresh A01 source intake, recorded Codex review and checkpoint-bound successor
pass all 17 pipeline checks while retaining partial payment 488 and residual732.
The request XLSX now asks only for the residual and preserves the bank reference.
All 180 request-pack, raw-input and assurance regressions pass without skips
(`a01-residual-assurance-regression.xml`). Privacy review remains current.
Vera Codex and Cowork packages rebuilt; both parity checks pass and all 18 Cowork
MCP servers initialize/list tools. Logs use the `a01-` prefix under the retained
`/private/tmp/vera-remediation-01a07083` evidence directory; package hashes are in
`a01-package-hashes.json`. No deployment or publication occurred.

Fresh A04 current-source intake also completed: two closed invoices against the
one grouped bank payment; its only failing check is two pending Codex reviews.
Evidence: `source-acceptance-a04-reviewed-current`. Artifact inspection and review
remain necessary before calling A04 accepted. These results do not close the
remaining T15-T19 acceptance requirements or the full integrated T09 gates.

A05 source-based corrected request and reviewed successor completed. The
request preserves later payment evidence while requiring balance confirmation
at cut-off. Both refreshed Vera package parity checks pass, including 18 Cowork
MCP startup/tool-list checks. Current privacy validator passes. Regression
report: a05-assurance-regression.xml; source acceptance: source-acceptance-a05-request-fixed.
The broader task requirements remain open.

## A06 rejection-output verification, 6 September

A06 now preserves the rejected bank reference and requests clarification of the
perimeter. Accountant output no longer displays equal cross-currency amounts as
zero difference or a high-confidence candidate. The actual source-based negative
acceptance asserts no allocation and preserves the failed currency control.
All 194 accountant, request, raw-intake and assurance regressions pass without
skips (a06-assurance-regression.xml). Privacy validator passes. Vera Codex/Cowork
rebuilds and parity checks pass, including 18 packaged MCP startup checks.

Open Item source-case evidence is now retained for A01-A08, with A06 explicitly a
rejection outcome. This does not fulfill T15 across Journal-Bank, Check Entries,
Passive Invoice or Journal Sampling. Those applicable workflow cases and the
independent/professional and native-host limits in ACCOUNTING_REVIEW_CASES.md
remain open. The next scope expansion is the Journal-Bank source intake and
reviewed final-artifact route, using the approved case expectations and its
actual accepted input contract.

Journal-Bank A04 explicit-list fix is documented and privacy-reconciled. Vera
Codex/Cowork rebuilt; both source parity checks and18 packaged MCP startup checks
pass. Logs: journal-bank-list-*-build/check.log. Its managed synthetic run is
complete with36 sealed artifacts;403 test cases have passing evidence across
full run plus the corrected stale-fixture retry.

Journal-Bank A01 current-source inspection and reconciliation now also executed:
bank488 and journal1220 with the same explicit reference. Both inputs qualified;
0 matches,1 unmatched per side. Evidence: journal-bank-a01-current. This does
not yet prove partial-allocation treatment or final review: inspect the bounded
residual context and reporting, distinguish unreconciled journal posting from
Open Item invoice obligation, and assess permitted partial allocation under the
actual Journal-Bank contract before changing behavior. No semantic worker or
manual matching override was used. The run remains active.

### 2026-09-06 — Journal Sampling locale and real managed review handoff

- Added complete Italian, French and German review catalogs. Three public-path
  regression cases pass (`sampling-locale-tests.log` in the retained temporary
  acceptance directory). The first test assertion incorrectly expected the
  fallback entry title; corrected it to the actual localized sampling-control
  title without changing production behavior to satisfy that assertion.
- Fresh managed synthetic run `sampling-check-entries-localized` successfully
  inspected, normalized and sampled all three source rows. Its bounded review
  context preserves Italian, exact source rows and the 1220/610/100 EUR values.
  This is a complete tiny population, not statistical or professional assurance.
- **Open integration failure:** native MCP `validate_journal_sampling_review`
  rejects that exact generated context with `model_review_context does not match
  the persisted Journal Sampling review`. The documented CLI layout writes the
  review under `outputs/sample`; MCP `resolveRunOutputDir` defaults to `outputs`
  when supplied only its declared `client_engagement` and `model_review_context`
  arguments. The context intentionally excludes physical output paths. This
  mismatch requires a bounded, verified way to resolve the persisted sample
  review; do not pass private payloads or weaken digest validation as a workaround.
- Evidence: `sampling-check-entries-localized/sampling-review-validation.json`
  and `sampling-review-validation.log` under
  `/private/tmp/vera-remediation-01a07083`. No sampling acceptance decisions or
  downstream Check Entries run were created. Package/privacy refresh and broader
  tests remain pending after the locale source change.

### 2026-09-06 — Sampling public-context resolution corrected

- MCP now resolves the exact persisted minimized context in either the managed
  output root or its documented `sample` child. It requires exactly one match,
  rejects symlinked directories/context files and multiply linked context files,
  and retains subsequent persisted-payload digest and transaction validation.
  It does not accept a caller-supplied arbitrary filesystem locator.
- Four native MCP integration cases pass with bundled Node enabled: read-only
  validation, save, apply, and rejection of duplicate matching contexts. These
  cases create real assured managed runs and supply only public-schema context
  arguments (plus decisions for writes). Evidence:
  `/private/tmp/vera-remediation-01a07083/sampling-public-context-tests.log`.
- The initial single-case run skipped because Node was absent from PATH; the
  subsequent explicit bundled-Node runs passed. The retained earlier synthetic
  acceptance run predates this implementation change and must be regenerated
  before final acceptance. Broader regression, packaging and downstream invoice
  support acceptance remain outstanding.

### 2026-09-06 — Fresh Sampling closure and Check Entries handoff

- Fresh synthetic sampling run `run_7120224864e5fa155921c340` in engagement
  `eng_5d041238a0f225c24c0fd404` passed public-context MCP validation, then saved
  and applied seven explicit Codex technical decisions with the context reloaded
  between writes. Three-row CSV/XLSX material replay and output-set validation
  passed. The decision notes exclude statistical inference, invoice sufficiency,
  independent professional review and native visual attestation.
- Studio Archive finalized 54 declared outputs and completed that synthetic run.
  A local model-data disclosure explicitly leaves unmeasured delivery extent
  unmeasured. Required semantic artifact IDs were retained.
- The public `start_check_entries_from_sample` bound the exact nine upstream
  artifacts plus two explicitly imported XML support receipts and started
  `run_7dddc73b362e3efd0433a721`. Public inspection succeeded: three journal rows,
  two FatturaPA invoices, no PDFs. Synthetic support scenarios intentionally have
  one matching total (1220), one differing total (600 versus 610), and one absent
  invoice (100). These minimal parser fixtures are not SDI-validity qualification.
  Party/sign policy and actual check outcomes remain to review and execute.
- Sampling full suite: 171 tests, 170 passed, one stale empty-cache-directory
  fixture failed, zero skipped (164 seconds). Changed that fixture to an actual
  unowned Python source file; focused retry passed. This is combined passing
  evidence, not a single fresh full-green run after the fixture edit.
- Evidence retained in `/private/tmp/vera-remediation-01a07083`: full JUnit/log,
  `sampling-unowned-retry.log`, `sampling-public-apply.log`,
  `sampling-public-finalize.log`, `check-entries-inspect.log`, and per-run JSON
  responses under `sampling-check-entries-public-context`.

### 2026-09-06 — Check Entries actual synthetic outcomes

- The public `run_checks.py` completed on the exact bound population/support
  batch: 3 checked rows, 2 manual-review rows and 1 missing-support row. Native
  MCP validation succeeded; selected bounded contexts were requested for those
  three cases only. Exact movement/invoice identifiers were requested to compare
  the authored synthetic identities.
- INV-10001: 1220/date/currency match, but no source-bound party/direction
  decisions were supplied, so approval is correctly withheld. INV-10002:
  invoice600 versus journal610 is recorded as an amount mismatch, together with
  the same missing party/direction evidence. INV-10003: no bound invoice support.
  No professional conclusion or unsupported party assertion was invented.
- **Open output defect:** the absent invoice is labelled `support_pdf`, with
  English text asking for a supporting PDF despite an Italian XML-support run.
  `_check_one_entry` supplies PDF-only fallback wording and
  `review_session._requested_support_document` hard-codes PDF in English/Spanish.
  Correct the generic missing-support request across all five supported locales
  while retaining precise PDF ambiguity diagnostics where PDFs actually conflict.
- Evidence: `check-entries-run.log`, `check-entries-bounded-review.log`, and
  `check-entries-case-context.json` under the retained acceptance directory.
  The Check Entries run remains unfinalized pending this output correction and
  additional source-bound positive qualification; no decisions were applied.

### 2026-09-06 — Missing support wording corrected and replayed

- Check Entries now requests a supporting document rather than prescribing PDF,
  with explicit English, Italian, French, German and Spanish request templates.
  A genuinely missing match across supplied evidence uses `support_document`
  and localized explanatory text. PDF ambiguity and XML relationship-review
  branches retain their specific diagnostics.
- Six focused missing-support/Spanish tests passed, including new Italian,
  French and German public-output checks. Initial collection failed because
  `request` is a reserved pytest parameter; renamed the test parameter only.
- Created an explicitly separate Check Entries run from the same completed
  sample and exact support receipts. Inspection/checks and bounded native MCP
  review succeeded. Its missing MOV-10003 now reads `Documento giustificativo
  del movimento MOV-10003` with an Italian explanation and `support_document`.
  Two manual-review rows and one missing-support row remain; the wording change
  does not invent party/direction evidence or turn discrepancies into approval.
- Evidence: `check-entries-missing-tests.log`, `check-entries-wording-run.log`,
  and `check-entries-wording-case-context.json` under the retained acceptance
  directory. Full Check Entries suite launched separately; its result is pending
  and must be inspected before claiming broader closure. Packaging/privacy
  refresh still outstanding for the latest Sampling and Check Entries changes.

### 2026-09-06 — Source-bound synthetic invoice decisions

- Separate run `run_ac2cfc473424180094e5ad12` uses the same closed sample and
  support receipts with explicit reviewed party/direction receipts for the two
  supplied invoices. The synthetic case definition is retained in
  `reviewed-scenario-basis.md`: authored purchase-debit postings from supplier
  IT01234567890; account 120100 is an arbitrary synthetic label, not inferred
  real-client accounting semantics. No receipt was invented for the absent third
  invoice. Receipts bind exact prepared entries and captured support artifacts.
- Public execution and selected native MCP contexts verify one `ok`, one
  `mismatch`, one `missing_support`. Even the `ok` row retains professional
  conclusion `pending_review`; this is mechanical evidence acceptance only.
- Additional reporting issue exposed: the known XML amount600 versus journal610
  is labelled `amount,missing_amount`, and `amount_found` is blank because code
  assigns it only when the amount comparison passes. The source amount is
  available in evidence facts. Distinguish an observed differing amount from a
  missing amount in a follow-up correction. Other XML review notes also remain
  English in the Italian run; the missing-document wording fix is narrower than
  complete Check Entries localization.
- Evidence: `check-entries-reviewed-run.log`, selected
  `check-entries-reviewed-case-context.json`, and reviewed recipe in this run's
  inspection directory. Full regression session 21197 remains live as last
  polled; do not change sealed implementation files until it finishes.

### 2026-09-06 — Amount observation regression captured

- Added a public `run_entry_checks` regression with exact invoice identity/date,
  reviewed party/direction receipts and invoice600 versus journal610. It fails
  on current code because `amount_found` is None rather than the observed600.
  It also requires the mismatch to exclude `missing_amount` when evidence exists.
  Evidence: `check-entries-amount-red.log`. This test is intentionally red pending
  the production correction; it was added after the ongoing full suite collected.
- Root cause inspected: the XML branch populates found amount/date only from
  successful comparison signals, and derives missing checks from those same
  successful signals. Observation, execution and passing comparison need distinct
  treatment. Preserve source currency and existing signed-comparison boundaries.
- Full regression session 21197 is still live, progressing beyond its first
  72 tests. Implementation files remain stable until that run completes.

### 2026-09-06 — Check Entries regression closure and observed-value fix

- The full missing-support-wording suite finished: 207 passed, zero failures,
  errors or skips, 730.741 seconds. Evidence: `check-entries-wording-full.xml`.
  Session21197 is terminal; do not poll it again. This precedes the next fix.
- Applied the reproduced XML observed-value correction: amount/date fields now
  retain extracted values regardless of successful matching signals; missing
  checks derive from absent usable evidence rather than failed comparison.
  Successful matching signals, currency comparability and mismatch status remain
  separate. Existing material-value lineage receives the actual observed value.
- The formerly red invoice600/journal610 regression now passes. The USD/EUR
  negative regression also passes, preserving mismatch rather than accepting
  equal nominal values. Evidence: `check-entries-amount-fix.log` (2 cases).
  Fresh synthetic replay and broader post-fix regression remain pending.
- A Clara-task coordination reply was rejected by automatic approval review
  because the destination/disclosure lacked explicit user authorization. No
  workaround was attempted. Incoming coordination says Clara work intends to
  edit Clara-owned code and later rebuild Clara packages; treat shared assurance
  and package-builder edits as potential concurrent work and preserve them.

### 2026-09-06 — Observed amount verified in saved outputs

- A fresh managed Check Entries run, bound to the completed sample and the same
  two exact support receipts, now emits invoice600, journal610 and signed
  difference-10 for MOV-10002. Its mismatch is exactly `amount`, without the false
  `missing_amount` flag. Both saved CSV and XLSX retain600; selected native MCP
  review also validates. Overall statuses remain `ok`, `mismatch`, `missing_support`.
- Evidence: `check-entries-observed-run.log`,
  `check-entries-observed-review.log`, and `observed-value-acceptance.json` under
  the retained synthetic acceptance directory. This is saved-output verification,
  not native visual or independent professional acceptance.
- Broader post-fix XML/FatturaPA/numeric-lineage tests launched with JUnit target
  `check-entries-observed-regression.xml`; session78828 remains to inspect. No
  packaging/publication claim is made, and overall T01–T20 closure remains open.

### 2026-09-06 — Post-fix evidence and Vera package refresh

- All18 selected XML/FatturaPA/numeric-lineage regression cases passed with
  zero skips (54.812 seconds). The additional observed-date mismatch case also
  passed: a differing invoice date is retained and not labelled missing.
- Reviewed the latest Sampling/Check Entries changes against their privacy
  surfaces and refreshed those two fingerprints. Complete Vera privacy validation
  passes. The first refresh returned failure only because the second component
  was still stale; that second refresh subsequently closed it.
- Rebuilt Vera Codex and Cowork candidates and ran their source-parity checks.
  Commands completed successfully. The initial Codex build lacked Node on PATH;
  repeated with the bundled Node runtime before successful build/check.
  Evidence: `vera-observed-build.log`, `vera-observed-cowork-build.log`,
  `vera-observed-check.log`, `vera-observed-cowork-check.log` in the retained
  temporary evidence directory. No deployment, merge or publication occurred.
- Release/package-wide tests, broader language completeness and full T01–T20
  acceptance remain open. These local candidate checks do not prove installed
  Cowork, Windows, OpenAI Platform or Marketplace behavior.

### 2026-09-06 — Package-wide results and public-source retention

- Package/update-notification suite:390 tests,387 passed,3 failed,zero skips.
  One failure is Clara archive drift: its current source includes newly added
  `case_exchange_safety.py` and `case_store.py` absent from the tested ZIP, matching
  the independently reported concurrent Clara work. Do not overwrite or rebuild
  another active task's unfinished source merely to hide this failure.
- The other two failures were extracted Clara renderer managed-environment setup
  failures due to sandbox DNS. An approved host-network rerun of those exact two
  test cases completed successfully. Evidence: `vera-package-network-retry.log`.
  The cross-product archive-parity failure remains unresolved; Vera's own prior
  source parity and18MCP-startup checks remain separately recorded.
- Retrieved the public Invitalia requirements HTML for L03/L04 through approved
  host network after sandbox DNS failed:HTTP200,242646bytes,SHA256
  `c5c67ee2c6f05a9d8df6fd7a79d22d1dc833e992a795edb82ade7d403cd78aa4`.
  Original HTML and retrieval receipt retained under
  `/private/tmp/vera-remediation-01a07083/professional-primary-sources`.
  This strengthens source retention; installed-workflow and independent
  professional qualification remain outstanding.

### 2026-09-06 — Check Entries review persistence exercised

- Native MCP save/apply succeeded on the fresh observed-value run. Decisions
  explicitly accept the mechanically supported synthetic entry, mark the10EUR
  discrepancy unclear, and request the missing MOV-10003 support document.
  Other artifacts remain unclear pending complete content/native-visual review;
  no blind acceptance or professional sign-off was substituted.
- Saved `final_artifacts.json` remains `blocked`, correctly reflecting unresolved
  review. This is a workflow outcome, not a request for further user approval
  of the authorized development work. No source rows were edited to clear it.
- Evidence: `check-entries-observed-apply.log` and
  `save_check_entries_decisions-observed.json` /
  `apply_check_entries_decisions-observed.json` under the synthetic acceptance
  directory. Full final-artifact review, declaration and ledger finalization
  remain outstanding; no completion claim for this run is made.

### 2026-09-06 — Correction: repeated new-run requests reused an ID

Direct comparison of saved start responses and context-location files shows
that wording, reviewed-policy and observed-value executions all used
`run_ac2cfc473424180094e5ad12`, despite each driver passing `new_run=True`.
Earlier descriptions of these as separate managed runs were incorrect. They
were fresh executions against updated source in the same running record.
Retained logs still demonstrate the stated results, but do not prove separate
immutable run histories. Trace and fix Studio Archive's explicit-new-run
behavior, then rerun the acceptance chain with verified distinct IDs.

Added an Italian `outputs/codex_run_review.md` to that run, explaining the match,
10EUR discrepancy, missing document, recorded decisions and qualification
limits. The note does not alter sealed check outputs. Run finalization remains
pending; do not treat this note as completion of the unresolved review.

### 2026-09-06 — Explicit-new-run identity corrected

- Root cause: `new_run=True` hashed the automatic request key into the same
  stored key on every invocation. With no caller retry key, later explicit new
  requests therefore replayed the first explicit-new run.
- `client_ledger.prepare_run` now generates a fresh random stored key for an
  explicit new request without a caller idempotency key. An explicitly supplied
  retry key retains existing replay semantics, including content-conflict checks.
- Both public API regressions pass: distinct IDs without a supplied retry key,
  and intentional replay with an explicit key. Evidence:`new-run-tests.log`.
  This shared Studio Archive source change needs broader ledger tests, privacy
  reconciliation and package regeneration; preceding package results predate it.

### 2026-09-06 — New-run identity verified against real ledger

- Full customer-folder ledger suite passes37tests (no failure shown).
- Two public `start_check_entries_from_sample` calls with identical exact inputs,
  `new_run=True` and no explicit retry key produced distinct IDs:
  `run_28e73811a847ce6098884bcf` and `run_af2ade65ec562e839339190a`.
  The second probe was explicitly cancelled with its record retained.
- The first new record successfully passed bound-input inspection. Its reviewed
  synthetic party/direction recipe was recreated for that execution; actual
  check results are retained in `check-entries-distinct-run.log`.
  This directly verifies the identity correction rather than inferring separate
  runs from distinct filenames. Prior reused-run history remains disclosed.
- Evidence: `new-run-ledger-full.xml`, distinct-a/distinct-b start responses and
  distinct-b cancellation response in the retained synthetic acceptance folder.
  Shared-ledger privacy/package refresh remains pending after this source fix.

### 2026-09-06 — Ledger privacy and Vera package parity refreshed

The new-run change affects local run identity only: no additional external
recipient or model-visible case data was introduced. Reviewed and refreshed the
Studio Archive privacy fingerprint; the complete Vera privacy validator passes.
Rebuilt Vera Codex and Cowork candidates. Both source-parity checks pass and all
18 packaged MCP servers initialize and list tools. Logs: `ledger-privacy-refresh`,
`vera-ledger-build`, `vera-ledger-cowork-build`, `vera-ledger-check`, and
`vera-ledger-cowork-check` under the retained temporary evidence directory.

The broader Studio Archive component/nested-source authorization suite is now
running separately (`ledger-component-regression.xml`). No release occurred.
Existing Clara-wide package drift remains a separate concurrent-work issue.

### 2026-09-06 — Archive regressions and obsolete run lifecycle

All32 Studio Archive component/nested-source authorization cases passed;
`ledger-component-regression.xml` is authoritative. The37 ledger cases and
these32 component cases cover separate scopes.

Cancelled four obsolete diagnostic records that still said running: initial
unsupported-layout sampling, corrected-account pre-locale sampling, pre-MCP-fix
localized sampling, and pre-reference-list-fix Journal–Bank A04. Also cancelled
the initial Check Entries baseline and its superseded reused-run record. All
outputs, applied decisions and error logs remain in place; cancellation is not
acceptance or deletion. Exact IDs and results are saved in
`obsolete-attempt-cancellations.json` and per-attempt cancellation responses.
The distinct Check Entries run `run_28e73811a847ce6098884bcf` remains the current
unfinished acceptance record. Completed earlier runs were not changed.

### 2026-09-06 — Distinct invoice run sealed for review

Native MCP validation/save/apply succeeded on the verified distinct run
`run_28e73811a847ce6098884bcf`. Post-apply CSV/XLSX inspection proves invoice600,
journal610 and difference-10 with exactly the amount mismatch. Applied decisions
retain the Italian request for MOV-10003. Final artifact status remains blocked
by unresolved review, as expected; see `distinct-run-acceptance.json`.

Created the local model-data disclosure without claiming measured provider
visibility. Declared the physical output set through Studio Archive finalization,
which moved the run to `ready_for_review`. This is artifact availability, not
professional clearance or a completed goal. No `complete_run` call was made.
Evidence: `check-entries-distinct-finalize.log` and the corresponding declaration
and finalization JSON retained beside the synthetic case.

### 2026-09-06 — Bandi L03 managed case started

Created a synthetic Studio Archive Bandi engagement and immutable import of the
retained Invitalia FAQ HTML. The intended registration is `official_faq` with
`clarifying` authority, not a substitute for a formal governing act. Before
initialization, the normal dependency check reported missing declared `strenum`;
the component requirements explicitly include `strenum>=0.4.15,<0.5`.
No dependency bypass or eligibility verdict was applied. Run/source records are
retained under `/private/tmp/vera-remediation-01a07083/bandi-l03-current`;
`bandi-l03-start.log` captures the failure. Continue with declared-dependency
setup, then initialize/register and execute the bounded case. This is an
environment prerequisite, not a new user-approval requirement for case proposals.

### 2026-09-06 — Bandi environment and source registration completed

Installed only the component's declared requirements into the repository
virtual environment through approved host setup. Its normal dependency checker
now passes. Resumed the existing run `run_21b16fbcff7912ed8d607566`, initialized
all six draft/register files, and registered `INVITALIA-FAQ-ON` with its exact
retained hash, source type `official_faq` and authority `clarifying`.
Evidence: `bandi-dependency-setup.log` and `bandi-l03-resume.log`.

No eligibility assessment, source-baseline approval or professional contribution
acceptance has been asserted. The next stage must use the workflow's bounded
intelligence packet and truthful fresh-session identity, and distinguish the
FAQ from the formal governing sources. This is execution progress rather than
completion of L03/L04 or T17.

### 2026-09-06 — Formal ON sources retained and bound

Retained the decree PDF (142480bytes,15/15pages with text,SHA256
`c6e9f6ceac6972dde84e378dcb11510e6955ff2e13c20cade23ecd5bf0f26cb6`)
and circular PDF (1533652bytes,26/26pages with text,SHA256
`bf169e99b3c98f118e0eaf4cd8abd0bc5ccdd348457d4ecd28560741b15d3077`).
Saved original files, per-page extraction JSON and receipts in the primary-source
folder. Text presence does not by itself prove complete visual extraction.

Prepared and initialized a distinct Bandi run with exactly these two sources
and the FAQ, preserving the earlier FAQ-only run's input boundary. Registered
all three; source authority and classifications remain proposed for review.
Located constitution-age and ownership provisions on decree page5 and circular
page3, section4.1. No complete current-law or professional acceptance claimed.
Evidence: `bandi-formal-start.log` and `formal-context-location.json` under
`bandi-l03-current`. Remaining work includes current amendment/applicability
review and normal bounded intelligence execution with truthful session provenance.

### 2026-09-06 — Bandi packet boundary inspected

The normal packet command succeeded for `SOURCE_INTERPRETATION` scoped to
`ON-DECREE-2020`. It returned one source, zero stored excerpts, empty requirements
and issues, and excluded unrelated evidence/assessment collections. The packet
is3164bytes and reports no silent truncation. It allows selected-official-source
reading; metadata alone is not substantive interpretation evidence.

Used an explicitly labelled current-thread technical inspection reference only.
No contribution was recorded and no fresh-session/provider-attestation claim
was made. The command logs JSON to stderr; the inspection driver's initial
stdout JSON parse failed, after which its retained stderr JSON was read.
Evidence: `bandi-l03-current/packet-inspection.stdout` and `.stderr`.
Remaining work requires actual source reading in a fresh contribution context,
truthful model identity and exact proposal review. Source metadata currently
uses provisional titles/issuer labels; correct those in a properly versioned
case rather than silently rewriting immutable registrations.

### 2026-09-06 — XML check narrative localization

Replaced the XML matched/missing-direction/mismatch branch's English fallback
for Italian, French and German with explicit localized descriptions. Existing
English/Spanish text and all status/evidence logic are preserved. A dedicated
copy helper chooses presentation text only; it does not classify evidence.
Public supported-entry output tests cover Italian/French/German, alongside the
amount/date observation regressions. Evidence:`xml-language-tests.log`.
This corrects the reproduced XML narrative issue; it is not a claim that every
Check Entries review/artifact label is localized. Latest package/privacy checks
predate this source edit and require refresh after validation.

### 2026-09-06 — XML language outcomes and candidate refresh

All9 public XML outcome/language combinations passed: supported, missing
reviewed direction, and amount mismatch in Italian/French/German. Status
assertions preserve the distinct outcomes; copy checks reject English fallback.
Whitespace validation of the recent source/test changes passes.

Refreshed Check Entries privacy fingerprint after review; Vera privacy validation
passes. Rebuilt Codex/Cowork candidates; both parity checks and18MCP startup
checks pass. Evidence: `xml-all-outcome-locales.log`, `xml-locales-privacy.log`,
`xml-locales-check.log`, `xml-locales-cowork-check.log`. This validates these
three XML messages, not every review label or the full T01–T20 scope.

### 2026-09-06 — Journal–Bank A02 actual reversal case

Executed the public managed Journal–Bank pipeline with bank+1220 and journal
+1220/-1220 carrying the same reference/date/currency/party. Explicit policy:
one-to-one,same-sign,same-party. Result: one positive amount/date match,
zero unmatched bank rows, and the negative1220 journal reversal retained as an
unmatched row. Selected native MCP contexts confirm both outcomes and pending
review. No source row disappeared and no whole-position settlement was asserted.

This differs from Open Item obligation cancellation semantics: the fixture here
contains journal bank-posting candidates, so technical preservation of the
negative residual is the observed result. Whether the positive posting is the
appropriate accounting counterpart remains a professional review question.
No decisions were auto-accepted. The retained missing-evidence request still
uses English despite Italian run language; broader Journal–Bank copy review
remains open.

Evidence: `journal-bank-a02-inspect.log`, `journal-bank-a02-run.log`,
`journal-bank-a02-review.log`, and bounded context under `journal-bank-a02-current`.
Managed run `run_9b014db29ecd30605ac1d204` remains pending review.

### Journal–Bank missing-evidence localization — 2026-09-06

Corrected the review payload's missing-evidence requests and reasons for Italian,
French and German on both unmatched bank and unmatched journal entries. Requests
identify the supporting journal/ledger entry or bank statement/payment evidence;
they do not prescribe a PDF. Existing English and Spanish branches are preserved.

Verification: the public `run_reconciliation` workflow passed three parametrized
locale cases (`test_run_localizes_missing_evidence_requests_on_both_sides`). Each
case verifies both sides, the `request_more_documents` action, and the requested
document copied into review evidence. This proves these request fields, not full
localization of the report or independent professional acceptance. The source
change requires a fresh privacy fingerprint/package rebuild before final package
parity can be claimed; previously built candidates predate this change.

### Journal–Bank locale package refresh — 2026-09-06

Reviewed the request-text-only change against the existing Journal–Bank model
context manifest and refreshed its source fingerprint. Complete Vera privacy
validation passes. Rebuilt Vera Codex and Cowork candidates; both source-drift
checks pass and 18 packaged MCP servers initialize and list tools. Logs are in
`/private/tmp/vera-remediation-01a07083/journal-bank-locales-*.log`.

The required package/update suites ran to completion: 390 tests, 387 passed,
3 failed, no skips (58.628 seconds; `journal-bank-locales-package-tests.xml`).
The failures are current Clara ZIP/source drift (including public_http.py,
case_exchange_safety.py, case_store.py and case-operations.md) and two extracted
Clara rendering tests whose declared-dependency bootstrap cannot resolve PyPI
inside the sandbox. These are not a green cross-product gate. Preserve the
concurrent Clara source work; do not rebuild its unfinished package merely to
hide drift. No release or publication performed.

### A07 Journal–Bank review package retained — 2026-09-06

Actual managed ambiguous-receipt case now has saved/applied review decisions and
35 sealed outputs, including model-data disclosure. All three unmatched entries
request evidence; ten artifact reviews remain unclear. Application correctly
withholds report readiness with zero target changes. Studio Archive status is
ready_for_review, not completed. See ACCOUNTING_REVIEW_CASES.md for run identity
and exact evidence locations. No further user approval of original expectations
is being requested; the remaining limitations concern execution/qualification.

### Changed economic amount invalidates prior mapping review — 2026-09-06

Strengthened the existing public Journal–Bank source-change regression. It now
covers both the original byte-only change and an actual bank amount replacement
from 80 to 90 while retaining the same date/reference and journal amount80.
Both cases first establish one match with the reviewed original source, then
require `ReconciliationBlockedError` with `mapping_review_required` and the
persisted audit's invalid/stale receipt explanation on replacement. Both pass;
evidence: `/private/tmp/vera-remediation-01a07083/journal-bank-changed-amount-tests.xml`.

This adds economic-change coverage to the existing source-binding test; it does
not claim execution of the full managed A08 100-to-110 case or independent
professional disposition of the difference. No production logic was changed.

### A08 same-engagement replacement evidence sealed — 2026-09-06

Verified stale-review rejection on bank100-to110 replacement within the original
engagement, preservation of original bank and journal bytes, and fresh-review
abstention with explicit EUR10 evidence requests. Finalized59 outputs to
ready_for_review, including honest model-data disclosure and both failed/fresh
execution artifacts. Independent physical SHA256/size/coverage audit passes59/59.
See ACCOUNTING_REVIEW_CASES.md and journal-bank-a08-same-engagement evidence.
This is unresolved-case technical acceptance, not final professional clearance.

### A05 workflow applicability clarified — 2026-09-06

Inspected Journal–Bank's actual relationship policy and CLI: symmetric date
window is supported; invoice settlement at an as-of cutoff is not. Original A05
is applicable to Open Item (existing execution evidence), not an executable
Journal–Bank cutoff acceptance case under its present contract. Recorded this
boundary in ACCOUNTING_REVIEW_CASES.md rather than fabricating a passing case
with different accounting meaning. Other pending acceptance remains unchanged.

### Passive Invoice native preflight revalidated — 2026-09-06

Read the Passive Invoice workflow and public run contract before source-case
execution. Its dependency preflight exits1: installed Codex is0.153.4 with SHA256
4ca47945439f9251fe35f4cbe071369192cd9a6c5a3a17b75c7a11ad548a9c7f,
which does not meet its qualified native Luna binary condition. Exact output is
retained in passive-invoice-dependency-preflight.log under the remediation temp
root. No native semantic audit launched and no model/pin substitution made.
This is a current execution-boundary failure, not evidence of matching accuracy.
Deterministic fixture preparation and review of unmatched/duplicate behavior can
continue independently; matched-invoice semantic qualification remains pending.

### Passive Invoice duplicate-only output regression — 2026-09-06

Added a public run_audit regression with two byte-identical XML files and one
booked ledger movement. It verifies zero worker invocations, two explicitly
flagged duplicate_candidate population records, persisted SQLite evidence and
both exception rows in the normal XLSX workpaper. A fixture runner configured
to fail is never called; no semantic worker result or host qualification is
simulated by this test. This extends existing matching-only duplicate coverage
through normal artifact writing. Together with existing ambiguity, missing
ledger and duplicate matching cases, four targeted tests pass. JUnit:
/private/tmp/vera-remediation-01a07083/passive-deterministic-cases.xml.
This is a self-contained regression, not the outstanding managed native audit.

### Passive Invoice full regression result — 2026-09-06

Full component test suite is terminal:89 tests,88 passed,1 skipped,0 failures
and0 errors (1.310 seconds). The sole skip is the explicitly opt-in native Luna
integration test; the current failed binary qualification preflight means it
is not counted as passing. JUnit and log: passive-current-full.xml/log in the
remediation evidence root. Includes the new duplicate-only full artifact-writing
regression alongside existing matching, semantic contract, recovery, evaluation
and workpaper tests. This remains fixture-based verification; actual native
semantic acceptance is outstanding.

### Histogram regression and package ownership — 2026-09-06

Complete distribution suite after explicit density/cumulative tests:21 tests,
20 passed,1 skipped,0 failures (4.099 seconds), histogram-density-final-tests.xml.
Inspected components.json: distribution-analysis is embedded in Clara, not Vera.
The component-selective package check therefore targets Clara and fails current
drift, including the histogram correction and concurrent dataset/case/runtime
changes. Exact list: histogram-package-drift.log. This is a related retained
reporting correction, not new evidence that all Vera financial pipelines pass.
Do not rebuild/declare the unfinished concurrent Clara package qualified merely
to remove this drift; its integration remains pending.

Re-read the user-referenced task “migrate to GPT-6 Astra”
(01a070ac-a728-7e90-ba29-66554a4c5abb). Its final recorded turn reports PR525 merge,
AML page integration, historical9168 passing tests/16 skips and80.22% coverage,
plus deployment e0b98921. That task is completed in the retrieved history.
These are historical task-reported results, not a fresh verification of this
working tree or permission to deploy the current remediation. No task message,
merge, deployment or publication was performed during this reconciliation.

### Financial Analysis current source-to-detail verification — 2026-09-06

Reran the three registered preparation packs plus the fiscal-year retargeting
case with retained outputs at financial-pack-current-evidence; all four tests
pass (financial-pack-current.xml/log). These exercise actual pack execution,
byte determinism, declared fiscal years and unavailable-denominator handling.
Separately inspected the synthetic monthly trial balance and reviewed account
mapping, then aggregated source values with Decimal and explicit presentation
multipliers without importing the preparation engine. All108 P&L detail values
across September2024–August2025 match the fresh output from144 source rows.
Evidence: financial-pnl-detail-review.json. This verifies mapped detail amounts;
subtotals, narrative claims, visual acceptance and independent mapping judgment
are not inferred from this check.

### Financial P&L full arithmetic and frozen-control check — 2026-09-06

Extended the independent Decimal calculation from raw source rows to gross
profit, operating expenses, operating income, pretax income and net income,
including the negative tax-benefit months. All168 monthly values match output;
all70 fiscal-quarter/year controls match sums of the relevant months. No pack
engine was imported. Script: verify_financial_pnl.py; evidence and output hash:
financial-pnl-complete-arithmetic-review.json in the remediation temp root.
Frozen fixture controls were not reverified against issuer filings; this is
arithmetic/source-fixture acceptance, not public-source authenticity review.
The prepared manifest explicitly keeps report_ready=false, and this preparation
output contains no narrative or rendered report to accept. Downstream report
claims/visuals and other financial pipelines remain separate outstanding work.

### Financial customer-concentration values and limits — 2026-09-06

Independent Decimal aggregation from frozen extracted facts/control CSVs verifies
all16 customer-concentration summary values, including reported-share sums,
receivable subtotals, coverage percentages rounded to six decimals, and sums of
squared reported shares. Missing2023 AR denominator remains blank/unavailable;
HHI rows explicitly describe incomplete reported-share contribution, not total
population concentration. No pack engine imported and no customer identity/risk
rating inferred. Evidence: financial-customer-concentration-review.json; script:
verify_customer_concentration.py. Source-filing verification and downstream
professional/visual acceptance remain separate.

### Financial working-capital source arithmetic — 2026-09-06

Independent Decimal calculations from frozen facts and the explicit reviewed
policy verify all5 stock periods,4 de-cumulated cash-flow quarters and5 stock/flow
bridge rows. The four quarterly residuals remain -298,-1827,3866,-1575 and the
annual residual166 (USD thousands), all explicitly unexplained. No residual was
allocated or forced to zero; distinct source captions remain governed by the
reviewed policy. Evidence: financial-working-capital-review.json and
verify_working_capital.py. This verifies arithmetic against frozen source facts,
not filing authenticity, residual explanations or downstream visual acceptance.

### Financial Analysis full contract regression — 2026-09-06

Full Financial Analysis suite passes24/24 with no skips/errors (1.390 seconds).
Inspected the negative assertions: an expected10/actual11 reconciliation cannot
retain a stale zero difference, and even a rehashed prepared manifest cannot
promote itself to report_ready. The suite also covers registered recipes,
source-bound crosswalk/model-use contracts, fiscal-year retargeting and actual
pack execution. Evidence: financial-analysis-current-full.xml/log. Together
with the independent arithmetic checks this strengthens preparation acceptance;
it does not establish downstream narrative or rendered-report acceptance.

### Sales Plan source/assumption lineage — 2026-09-06

Full preparation suite passes24/24, no skips (sales-plan-current-full.xml/log).
Retained current execution outputs under sales-plan-current-evidence. Inspected
four source rows, explicit China+8% units/USD-5% FX assumptions, proportional
sales discount/COGS policy and2025-to2026 period mapping. Independent Decimal
calculation verifies80 numeric cells across8 Actual/Plan rows, including FX,
net sales and gross margin, with source IDs/period lineage preserved. Evidence:
sales-plan-source-review.json. Synthetic fixture reviewer labels are fixture
metadata, not real professional forecast approval. Rendered report acceptance
and remaining broader T16 cases are not implied.

### Management Control current regression review — 2026-09-06

Full Management Control suite completed successfully; exact counts/timing in
management-current-full.xml/log. Inspected zero-revenue assertions: costs remain
770, total margin -770, undefined customer/service percentages are omitted as
metrics, and both Markdown/HTML explicitly explain zero-denominator margins
while retaining service losses450 and320. Same-date conflicting cash balances
stay unavailable in either source row order. These assertions cover the original
missing/zero/order defects rather than merely asserting successful execution.

The commentary test links its revenue2400 statement to the existing metric and
checks unknown-ID rejection; its hypothesis remains a hypothesis and result is
draft_pending_professional_review. This linkage test does not prove arbitrary
narrative text agrees with metric values. Broad claim-to-source and actual visual
acceptance remain outstanding; no unsupported semantic guarantee is inferred.

### Management narrative population wording corrected — 2026-09-06

Generated retained baseline and zero-sales-detail Markdown/HTML reports at
management-narrative-evidence. Direct report reading found the unqualified claim
“total revenue is zero” beside ledger P&L revenue2400: this synthetic negative
case changes only the separate Sales table, not GL. Corrected the concentration
reason to “the supplied sales-detail rows total zero revenue”, preserving all
amounts/statuses. Both HTML/Markdown regressions require this population-specific
reason and retain2400; zero-revenue calculation plus both render tests pass.
This is a wording correction, not a new assertion that separate GL/sales sources
reconcile. Fresh package/privacy refresh and regenerated report review remain
pending after the source change.

### Management wording regenerated and Vera packages refreshed — 2026-09-06

Fresh reports at management-narrative-corrected explicitly distinguish zero
sales-detail revenue from ledger revenue2400. Baseline and negative-case metric
maps are byte-value equivalent before/after. Full component suite33/33 passes,
no skips. Reviewed the text-only change against Management Control's existing
model-context manifest, refreshed its fingerprint, and complete Vera privacy
validation passes. Rebuilt Codex/Cowork Vera candidates; both drift checks pass,
with18 MCP servers initialized/tool-listed. Logs: management-wording-*.log and
management-wording-full.xml. No deployment/publication. Cross-product package
suite remains separately constrained by known concurrent Clara drift; these
Vera-specific checks do not claim cross-product closure.

### Variance Analysis current regression evidence — 2026-09-06

Full component suite passes67/67, no skips/errors (25.074 seconds), recorded in
variance-current-full.xml/log. Coverage includes scenario/calendar/rolling
period contracts, localized titles/review outputs, bounded model-use receipts,
small-multiple residual rows and chart reconciliation checks. Inspected explicit
negative-change assertions: total delta-50 with price0, volume-50, mix0 and
component reconciliation within1e-6. Separate tests retain chart reconciliation
within1e-6. These are scoped synthetic regression facts, not blanket evidence
that every decomposition or rendered variant is correct. Independent retained
source-to-effect checks and broader visual acceptance remain outstanding.

### Variance independent source-effect review — 2026-09-06

Retained the declared product-grain decline case and recalculated effects with
Decimal from four source rows. Product prices remain10 and20; unit changes+5
and-5 yield effects+50 and-100, totaling-50. Source sales300-to250 reconcile to
the reported delta and volume effect; price/mix effects remain zero under this
declared bottom-up method. Aggregate units stay20, so the interpretation must
refer to product-level changes, not an overall unit-count decline. The generated
summary explicitly retains calculation grain category/product and reports the
same-50 without adding a causal claim. Evidence: variance-source-effect-review.json
and variance-source-current. Other decomposition/visual cases remain open.

### 2026-09-06 — Revalidated the quoted missing-support defect

The user asked about the earlier English/PDF defect message. Current source in
`check-entries/scripts/review_session.py` already provides a generic Italian
supporting-document request. Re-ran the public-output regression
`test_missing_support_request_preserves_locale_and_document_choice`: all three
Italian/French/German cases passed. These checks inspect `review_payload.json`
and preserve `support_document` as the missing-evidence classification. The
quoted open-defect message is historical, superseded by the correction and
replay recorded above. This closes that wording defect only; missing evidence
and professional review remain separate from output-language correctness.

### 2026-09-06 — Variance decomposition ladder typography correction

Inspected the retained 1640x580 ladder and all three waterfall-family reference
images from the fixed reference manifest. The ladder mixed title, panel, category
and value sizes. Its plugin title adapter now sets a single 13px font across the
ladder's Plotly title, axes, annotations, legend and waterfall text. A figure
metadata value carries that size into the Pillow fallback; other chart types
retain their defaults. No calculation or trace values changed.

The public bottom-up-mix fixture regenerated a readable ladder without visible
clipping, retaining baseline300, comparison250 and product-grain units effect-50.
Artifact: `/private/tmp/vera-remediation-01a07083/variance-font-current/test_variance_plugin_uses_bott0/variance/pvm_decomposition_ladder.png`.
Full file regression: 66 passed, one Node-dependent MCP test skipped because
Node was absent from PATH (`variance-font-full.xml`, 25.489s). Re-running that
exact MCP test with the bundled Node directory in PATH passed. Black formatted
the two changed source files. Native Plotly export visual acceptance and updated
privacy/package integration remain pending; this is not a release claim.

### 2026-09-06 — Native ladder export fails visual acceptance

Host-Chrome execution of the same synthetic public pipeline completed and reports
`plotly+kaleido`, but direct PNG inspection shows missing waterfall rows and
clipped content. The numerical regression passing does not qualify this PNG.
Retained native artifact under `variance-font-native/test_variance_plugin_uses_bott0/variance`.
A separate capture driver retained the figure JSON at
`/private/tmp/vera-remediation-01a07083/variance-native-inspection/ladder.figure.json`:
three horizontal waterfall traces, all seven category labels, encoded float64 x
arrays, fixed x extent[-36,336], auto-inferred y-axis type, width845/height580.
Those observations narrow investigation to native figure/export interpretation;
no root cause is established yet. The Pillow artifact remains readable. Package
refresh is deferred until this native output defect is resolved. Capture driver
initially needed repo PYTHONPATH; rerun with that path completed successfully.

### 2026-09-06 — Native ladder category-coordinate defect corrected

Controlled native-export probes showed that explicit category axes and decoding
binary numeric arrays did not independently restore the missing rows. Replacing
the year-string percentage annotation coordinate with the final category index
restored all seven rows. The renderer now declares category axes and anchors the
percentage annotation to `len(variance_array)-1`, rather than the numeric-looking
comparison label. The actual public synthetic run with host Chrome passed and
its PNG shows baseline300, final250, and each decomposition's -50 contribution.
Retained output: `variance-native-category-fixed/test_variance_plugin_uses_bott0/variance/pvm_decomposition_ladder.png`
under the acceptance directory. Source x-array encoding was not changed.
The native title still touches/clips the top boundary and the transparent
background differs from the white fallback; native visual qualification remains
open for those observed presentation issues. No packages were rebuilt yet.

### 2026-09-06 — Native ladder title/background corrected

The ladder-only title adapter now uses y0.95 and an opaque white canvas. Its
recorded title coordinate comes from the actual figure rather than a stale
constant. Host-Chrome public fixture execution passed; direct inspection of the
fresh 845x580 native PNG confirms unclipped title, all seven rows, three panels,
and unchanged displayed amounts. The bounded example passes visual review;
see VISUAL_ACCEPTANCE.md. `git diff --check` passes for the two changed render
files. Package/privacy integration still needs completion after source review.

### 2026-09-06 — Vera ladder package integration

Reviewed the variance workflow's declared model-context and external boundaries
against the presentation-only edits: no new input, model-context class, network
route, or persistence behavior was introduced. Refreshed the variance-analysis
fingerprint; the complete Vera privacy register validates. Built Vera Codex and
Cowork packages from source and ran both --check commands successfully. The
Codex package check initialized and listed tools for all18 packaged MCP servers.
Logs in `/private/tmp/vera-remediation-01a07083/`: `variance-vera-build.log`,
`variance-vera-cowork-build.log`, `variance-vera-check.log`, and
`variance-vera-cowork-check.log`. No deployment or publication performed.

Package/update-notification regression finished: 390 tests, 387 passed, 3 failed,
zero skipped (58.630s; `variance-package-tests.xml` and `.log`). Inspected each
failure: global package parity fails on Clara entries absent from its current
ZIP (including decision_narrative, public_http, case_exchange_safety and reporting
execution modules); two extracted-Clara render tests fail while resolving PyPI
for declared Polars dependencies in the restricted environment. These failures
are not Vera package parity failures. Concurrent Clara sources/packages were
not overwritten. The combined suite is not green; Vera's two direct package
checks and all18 MCP startup checks passed as recorded above.

### 2026-09-06 — Centrale Rischi current synthetic source review

Full current `test_centrale_rischi_review.py`: 30 passed, zero failed/skipped,
2.240s (`centrale-current.xml`). Retained all test artifacts under
`/private/tmp/vera-remediation-01a07083/centrale-current`. Inspected the public
benchmark test's manifest: this is one page-level extraction case and one
analysis case over a five-page synthetic PDF, not the complete five-source
repository gold corpus. Expected rows and values were not changed.

Read PDF page1 directly with pdfplumber and compared it with the saved analysis:
current exposure45000, earlier corrected exposure60000 retained as previous and
excluded, granted45000, operational45000, available resources0, overrun0.
PFN/EBITDA, Debt/Equity, DSCR and month-on-month change remain unavailable with
reasons. The independent comparison receipt hashes source and analysis and
passes ten explicit checks (`centrale-source-review.json`). This supports the
represented correction-history case only; full gold corpus, rendered-output
review and professional interpretation acceptance remain open under T18.

### 2026-09-06 — Exact-hash Centrale Rischi gold source recovered and replayed

Located the archived synthetic eight-page report in the existing Test Vera
engagement. Its SHA-256 exactly matches the repository gold manifest:
`754e623a93f0c0194ccece0f71a62bdb5ae9b0fd0abbec519832fd7970fbdda4`.
Read-only use preserved the engagement. Four other corpus sources were not found
in the searched local directories. A temporary source-selected manifest retains
unchanged expected extraction/analysis cases and the synthetic semantic rubric;
selection.json records the original manifest hash. No repository gold asset or
expected outcome was edited.

Actual run_gold_benchmark.py execution passed the full eight-page synthetic
case: 1/1 extraction, 1/1 analysis; utilized195000, granted207000, CR margin12000,
month-on-month change-55000, zero overrun, two excluded previous corrections.
Seven exposure rows, one exposure-linked guarantee, one separate guarantee
received, four information requests and one inframonthly event are retained.
Row-order, previous-record exclusion and auxiliary-population invariants pass;
HTML/XLSX/context structural checks pass. Status remains
`deterministic_pass_semantic_review_pending`: no professional or commentary
approval was invented. Evidence: `centrale-gold-synthetic/run/benchmark_receipt.json`
and run.log under `/private/tmp/vera-remediation-01a07083`. Full five-source corpus
coverage and rendered/semantic acceptance remain open.

### 2026-09-06 — Source-linked Italian Centrale Rischi commentary

Read native text from all eight synthetic gold PDF pages and the generated facts
report. Authored six Italian observations covering latest exposure, aggregate
category movements, two previous corrections, the July event after the June
snapshot, separate third-party guarantees, and unavailable financial ratios.
Included four specific follow-up questions and explicit synthetic/model-review
limits. No causal recovery, immediately spendable liquidity or bank rating is
claimed. Source pages and existing metric/row references accompany the claims.

The production finalize_commentary function accepted the draft's schema and
reference closure and rendered Markdown/HTML in the existing synthetic benchmark
analysis directory. Status remains `draft_pending_professional_review`.
Evidence: `centrale-gold-synthetic/write_commentary.py` and generated
`run/analysis/analysis_synthetic_full/centrale_rischi_commentary.json`,
`centrale_rischi_report.md`, `centrale_rischi_dashboard_reviewed.html` under the
retained acceptance directory. This is authored source-grounded commentary,
not an independent semantic score or professional approval. Visual inspection
of the final HTML and independent semantic acceptance remain outstanding.

### 2026-09-06 — Centrale Rischi final HTML visual and control checks

Actual host-Chrome review of the synthetic gold commentary HTML passed at desktop
and390px mobile widths. No page overflow; all nine table containers support
horizontal scrolling to hidden columns. Six evidence disclosures exist; opening
the first exposes the exact referenced metric IDs. Draft status, limitations,
unavailable ratio explanations and Italian commentary remain readable. Evidence
and scope are recorded in VISUAL_ACCEPTANCE.md and `centrale-gold-synthetic`
under the retained acceptance directory. No renderer source change was needed.
Independent semantic/professional acceptance and four remaining corpus sources
are still outstanding.

### 2026-09-06 — XBRL boundary regression scope verified

Ran golden-case, XBRL conformance runner, taxonomy and file-security test modules:
24 passed, no failures/skips,3.051s (`xbrl-boundaries-current.xml`). Artifacts
retained under `xbrl-boundaries-current`. Inspected tests: checksum mismatch and
archive traversal reject; non-XBRL roots reject; Arelle error, severe and
calculation-inconsistency messages reject even with process exit0. Scanner
arguments remain separate and nonzero scanner verdicts reject.

The golden test builds a synthetic catalogue and rule packs; conformance runner
tests stub engine behavior. These results establish boundary/runner contracts,
not actual official-taxonomy or complete Arelle conformance qualification.
The PCI registry explicitly marks the official package not bundled and pins
SHA-256 c24b86375529469ca0be9a06b231fbb05da18df99fa36a1db2e587ab51e2f0f1.
No taxonomy assets or expected cases were changed. Real package/catalogue
qualification remains an outstanding T18 action.

### 2026-09-06 — Official PCI taxonomy and full XBRL golden suite

Downloaded the registry's official PCI-2018-11-04 ZIP into the temporary evidence
folder, verified SHA-256 c24b86375529469ca0be9a06b231fbb05da18df99fa36a1db2e587ab51e2f0f1,
and built the real catalogue:2399 concepts across ordinary, abbreviated and
micro entry points. `source-receipt.json` records URL, package/catalogue hashes
and installed Arelle version. No official taxonomy content was bundled/published.

Ran the unchanged 24-case golden suite with this catalogue and package:24/24
PASS. Inspected actual Arelle receipts: the engine executes offline validation
with XBRL2.1 calculations and reports PASS. Boundary G21 records unsupported
IFRS. G20 retains the mismatch and the fixture's explicit professional filing
treatment rather than selecting a filing route itself. Its `golden-reviewer`
metadata is synthetic regression input, not real independent professional
approval. Other cases retain their individual expected outcomes in the run
manifest. This supersedes the prior missing-real-taxonomy qualification gap for
this golden suite; it is not full XBRL consortium conformance or legal filing
certification. Evidence: `/private/tmp/vera-remediation-01a07083/xbrl-official-current/`
`source-receipt.json`, `catalogue.log`, `golden.log`, and
`golden/golden-run-manifest.json` plus per-case artifacts.

### 2026-09-06 — Real XBRL2.1 conformance suite passed

Downloaded the official XBRL-CONF-2025-07-16 package to the temporary evidence
folder; checksum matches registry00462b833fd064108d0781c601bf2b186db9b85ef950030df9db3ef2f68455e9.
Executed the shipped conformance runner with real Arelle2.42.1, offline,
XBRL2.1 calculations enabled:606/606 variations PASS, zero failed, exit0.
Independently read all606 CSV status rows and verified report/log SHA-256 values
against conformance-manifest.json. No stubs or expectation changes were used.
The runner removed its extracted suite after success as designed; the downloaded
ZIP, CSV, log and manifest remain retained under
`/private/tmp/vera-remediation-01a07083/xbrl-official-current`.

This closes the missing actual standards-suite execution for this local Arelle
version and pinned suite. It does not establish a proprietary bank rating,
professional accounting review, filing acceptance, or another host's runtime.
No source/package/publication changes were made in this qualification step.

### 2026-09-06 — AML uncertainty and durable-record checks

Ran current test_vera_aml_review.py:21 passed, zero failures/skips,1.021s;
retained receipts and outputs under `aml-current`, JUnit `aml-current.xml`.
Inspected the actual archive-bound CLI memo: draft_for_review; payer differs
from lender; missing mandate remains a request; screening is explicitly not
performed; hashes are not represented as evidence truth or compliance proof.
Tests also cover source tampering, unknown references, wrong workflow/client,
stale proposal decisions, unresolved issue dispositions and successor history.

Scope limitation from source inspection: the tests supply authored loan findings
and an illustrative legal-basis record. They test preservation/binding, not the
substantive quality of a new AML assessment or ownership reconstruction. No
current-law verification or professional approval follows from that fixture.
Ownership/control ambiguity cases and independent substantive AML acceptance
remain open under T18; no automatic approval was manufactured.

### 2026-09-06 — Archive-bound AML ownership ambiguity case

Created two explicitly synthetic originals: dated ownership extract (Holding
Beta60%, Persona Uno40%) and a client declaration naming Persona Due as sole
beneficial owner without the holding-chain/control documents. Read the AML
professional method and verified CNDCEC March2026 operational guidance, PDFpage5,
for case-specific judgment methodology; no article-level legal conclusion is
claimed. Authored an Italian assessment distinguishing documented shares,
client assertion, alternative explanations, missing evidence and limits.

Actual Studio Archive receipt preparation and shipped AML CLI execution passed.
The content-addressed record remains draft_for_review, calculation unavailable,
with both original hashes and locators. It does not confirm a beneficial owner,
infer suspicion, report negative screening or create a professional decision.
Targeted follow-ups request the holding chain and basis of the declaration.
Evidence: `/private/tmp/vera-remediation-01a07083/aml-ownership-current/` execution.json,
cli.log and referenced managed outputs; reproducible driver aml_ownership_case.py
in the parent acceptance directory. This is a model-authored synthetic case, not
independent substantive acceptance. Run-level model-data disclosure/finalization
and successor evidence review remain pending; no no-model receipt was created.

### 2026-09-06 — AML ownership run finalized with honest model-data disclosure

Generated the normal model-data JSON/Markdown, stating that both complete
synthetic originals and authored review entered the main Codex context; exact
provider extent is not independently measurable. No local-only/no-model claim.
Declared all physical outputs and invoked Studio Archive finalize_run: lifecycle
ready_for_review, while the AML content remains draft_for_review with ownership
unresolved. Verified every declared artifact hash/byte count and exact coverage
of the output directory (`artifact-integrity.json`). Evidence under
`aml-ownership-current`: model-data-request.json, output-declarations.json,
finalize-result.json and artifact-integrity.json. Source fixture helper labels
the workflow version test-version, so this is current-source lifecycle evidence,
not an installed-package runtime identity claim. No professional decision or
complete_run was recorded; the review history remains available for a successor.

### 2026-09-06 — AML successor preserves history and limits

Imported a third synthetic document documenting Persona Due's100% holding in
Holding Beta, plus the finalized previous record, into the same engagement.
Actual current-source AML CLI execution passed with the module's real manifest
version. The successor links the exact previous record digest; old bytes remain
unchanged. Its revised narrative distinguishes the now-supported holding chain
from unresolved sole-owner/control assertions and Persona Uno's unchanged40%
direct share. Status stays draft_for_review, with no professional decision.

Generated successor-specific model-data disclosure and finalized its outputs to
ready_for_review. Evidence in aml-ownership-current: successor-execution.json,
successor-cli.log, successor-model-data-request.json and successor-finalize-result.json.
The initial driver read the portable context as if hydrated and stopped with
KeyError input_bindings before imports/preparation; using the already-returned
hydrated finalization context corrected the driver only. No production change
was needed. Independent substantive/professional acceptance is not implied.

### 2026-09-06 — Browser contract checks and current skill dependency gap

Inspected shipped browser runtime, standard acceptance fixture, prior validated
synthetic capability and execution skill. The skill explicitly requires installed
chrome:control-chrome and forbids standalone Playwright/profile/CDP substitutes.
Searched installed skill roots/cache for that skill; no matching SKILL.md found.
The current session exposes unified CUA, but equivalence to the required
persistent connected-Chrome runtime has not been established. No standalone
browser or hand-written fixture was substituted for portable acceptance.

Current Node execution/discovery contract tests pass (browser-current-contracts.log).
They use FakeTab/FakeLocator and verify boundary enforcement/recovery mechanics;
they do not close changed-page/login/origin live acceptance. T19 remains open for
reconciling the documented Chrome dependency with the available controller and
then executing the changed-state cases through the shipped runtime. Existing
successful replays remain historical evidence for their recorded environment.

### 2026-09-06 — Repository-wide gate audit in progress

Launched the Makefile's coverage command against current tests, with JUnit/log
`integrated-current.xml`/`.log`. Formatting gates use check-only mode to avoid
rewriting concurrent work. Mypy command exits0 for132 files, but inspected
pyproject.toml has global ignore_errors=true; this is not proof of type
correctness and remains a T09 gate weakness. Bandit exits1 for30 low-severity
findings, zero medium/high, no skipped files. Isort check exits1, including661
findings in old tmp copies plus current source/test/module/package findings;
no files were formatted. Black check and coverage run remain live at this
checkpoint (sessions87156 and48596 respectively); final results must be read
from those handles, not inferred from partial logs. No fresh green gate claim.

### 2026-09-06 — Integrated gate results and actionable plugin typing issue

Black check finished exit1:289 files would reformat,2816 unchanged. Unsuppressed
read-only Mypy audit reports806 errors in65/132 src files; temporary config only,
repository settings unchanged. This is distinct from the targeted plugin gate.
The actual tests/plugin_runtime.mypy.ini gate checks20 plugin files with
ignore_errors=False and reports one error: inspect_sources.py:173,
_PublicHTTPSHandler._context unknown to stubs. Inspected the running Python
HTTPSHandler source: its constructor initializes _context and https_open passes
it to do_open. A type declaration matching that inherited TLS context is the
identified narrow correction; no suppression/runtime policy change required.
Targeted plugin Bandit -ll exits0 with zero medium/high findings (five low).
Coverage run48596 remains live, now emitting test progress; preserve source
until this run ends and inspect its exact result before editing or restarting.

### 2026-09-06 — Integrated coverage run confirmed progressing

Re-polled the exact live session48596; it remains active. The log advanced from
1% to2%. Read-only process inspection observed the Python process19486 at98.5%
CPU with4m12s CPU time over5m13s elapsed, consistent with active computation;
this is not evidence of a dead process or permission to restart the run.
The queued type-only TLS-context correction remains unapplied so this task does
not change governed source during execution. Black is terminal exit1 as recorded;
no additional formatter process remains tracked by this task.

### 2026-09-06 — Current Computer Use Chrome connection verified

The user clarified that Chrome is now managed under Settings → Computer Use →
Google Chrome. Live `cua.getState()` returned an extension-backed Chrome browser;
selecting it exposed the documented `tab.playwright` API. Therefore the earlier
missing-separate-plugin finding is superseded: Chrome is available. The source
skill still names `chrome:control-chrome`; its setup instructions need migration,
while preserving the existing-profile, Chrome-only DOM execution boundary.

Both shipped browser preflights passed (component 0.5.7). The shipped fixture ran
on its reported localhost origin, port 59751. A fresh tab in connected Chrome
showed the expected heading. Through the documented Playwright API, filling and
submitting synthetic client code, selecting Invoice, checking Reviewed, and
clicking Prepare package produced exactly `Package ready: Invoice; reviewed yes`.
The Download synthetic ZIP link was present and `downloadMedia()` returned without
error. That API supplied no file receipt: downloaded bytes/hash are not verified
by this observation. This proves current-controller interaction, not capability
executor replay, changed-selector recovery, or full T19 qualification.

AML successor artifact verification also passed for all five declared outputs,
with no undeclared outputs and the predecessor still matching its finalized hash.
Receipt: `/private/tmp/vera-remediation-01a07083/aml-ownership-current/successor-artifact-integrity.json`.
The integrated pytest process (session 48596) remained live when polled; its log
had advanced to 5%. No replacement test process was started.

### 2026-09-06 — Chrome setup source migration

Updated the browser module skill, discovery playbook, capability contract, Vera
wrapper and privacy boundary wording to use Settings → Computer Use → Google
Chrome. A separate Chrome plugin is no longer required. The executable boundary
remains extension-backed `tab.playwright`; native accessibility, coordinates and
non-browser steps remain outside portable execution. Updated the existing
instruction-contract assertions accordingly. All 69 browser capability tests
passed (exit 0), logged in `chrome-settings-tests.log` under the remediation temp
root. Refreshed browser-automation privacy hashes; the full Vera privacy register
passed. Package regeneration is pending this source change. The full-suite
session 48596 was already running before these edits, so its eventual receipt
must not be presented as an unchanged-source final qualification.

### 2026-09-06 — Chrome migration packages and source transport type check

Added the inherited `_context: ssl.SSLContext | None` declaration to the public
HTTPS handler in deep-research-validator. This is type metadata only; transport
behavior is unchanged. The configured plugin Mypy gate now passes all 20 files.
The complete deep-research-validator plugin test file passed (exit 0;
`source-inspection-type-tests.log`). Refreshed its privacy source hashes; the full
Vera privacy register passed.

Rebuilt Vera Codex and Cowork packages with the bundled Node runtime available.
Both source/package drift checks passed, including initialization and tool listing
for all 18 packaged MCP servers. Initial build attempts without Node on PATH
failed explicitly; the successful builds supersede those attempts. Package and
update-notification regression tests were started separately; their result is
pending. Full-suite session 48596 remains a pre-edit baseline, with failures
already visible in progress output and no final summary yet observed. No publish,
merge or deployment was performed.

### 2026-09-06 — Package regression results inspected

The 390-test package/update suite completed: 385 passed, 5 failed, no skips or
errors, 74.540 seconds (`chrome-settings-package-tests.xml`). Failures were:
Clara source/ZIP entry drift; Vera Platform upload wrapper drift; two extracted
Clara render cases unable to resolve pypi.org for declared Polars dependencies;
and Clara public version 0.1.177 trailing locally installed 0.1.180. These are
separate release/runtime surfaces, not five demonstrated Vera runtime defects.
The remaining old Chrome-control privacy instruction was corrected and both
Vera install-package checks passed again with 18 MCP servers. The Platform
upload ZIP requires the builder's explicit `--chatgpt-upload` path and is being
rebuilt and independently checked; no Platform submission is authorized or made.

The explicit Vera Platform upload rebuild and source-drift check subsequently
passed, and the exact previously failing approved-card-copy regression passed
(exit 0). Thus the identified Vera upload mismatch is corrected; the four Clara
failures above remain unresolved by this action.

### 2026-09-06 — Intake document-kind boundary acceptance

Executed the public `parse_structured_fiscal_fields` entry point against a
synthetic mixed population: a cover letter misleadingly named F24.pdf, an
explicitly unreadable PDF, and a source whose extracted text file is missing.
The current assistant reviewed the cover letter as unsupported rather than an
actual F24 (model review, not professional approval). All three sources retained
individual dispositions in input order; statuses were reviewed_unsupported,
unreadable, unreadable, and no fiscal fields were invented. The lexical F24 hint
remained visible separately. After changing the cover text, reuse of the prior
text-hash-bound decision raised the expected stale-decision error. Four boundary
checks passed; receipt and disposition artifact are under
`/private/tmp/vera-remediation-01a07083/intake-kind-current/`.
This is direct public parser evidence for T12/T18, not a complete managed intake,
all-language qualification, or professional acceptance. The full-suite process
48596 was separately polled live and had advanced to 12%; no restart occurred.

### 2026-09-06 — Managed intake handoff verification

The complete `test_vera_new_client_pipeline.py` and
`test_vera_new_client_taxonomy.py` files passed: 18 tests, exit 0, no skips
(`intake-handoff-current.xml` and `.log`). Inspected the actual cases: source
preparation runs through its CLI, the MCP applies synthetic review decisions,
SHA-256-sealed phase-one outputs are promoted into the same engagement's
new-client workflow, and final packaging retains run IDs and relative artifact
references. A directly imported manifest and another workflow's artifact are
rejected without writing the promoted input. A mismatched ledger run ID is
covered separately in the same file.

The helper accepts every fixture review item using `reviewer-e2e`; that is
synthetic test input, not independent professional review or user authorization.
The successful handoff therefore qualifies mechanics and identity boundaries,
not substantive fiscal conclusions. No production change was needed for this
run; the four direct parser boundary checks from the preceding checkpoint remain
separate evidence.

### 2026-09-06 — Repeatable intake disposition regressions

Added `tests/plugins/test_fiscal_document_dispositions.py` with four public-parser
cases: misleading F24 filename overridden by reviewed cover-letter classification,
unreadable source retained, missing extracted text retained, and stale text-bound
review rejected. Uses synthetic records and local temporary text only. No
production behavior was changed. The four cases passed, and the file was Black
formatted with Isort check passing. A combined run with the existing managed
handoff and taxonomy cases verifies coexistence rather than relying solely on
the earlier ad hoc probe; its receipt is `intake-regressions-current.xml`.

### 2026-09-06 — Archive apply and rollback qualification

Ran the complete archive component and wrapper test files: 71 passed, zero
failures/errors/skips, 2.490 seconds (`archive-current.xml` and `.log`). Inspected
public apply/rollback tests: original bytes survive normal rollback; reviewed
source changes reject application; rollback refuses to overwrite a later revised
destination and retains its journal; injected interruptions after physical moves
recover using physical hashes and repeated rollback remains idempotent. The
matrix also injects file-boundary interruptions and competing operations.
These execute local filesystem mutations on temporary synthetic clients, not
merely schema checks. Explicit approval records are synthetic fixture inputs.
Google Drive cases use a fake gateway and do not constitute live Drive connector
acceptance. No user archive was reorganized. The full-suite session 48596 remains
live and had advanced to 16% at this checkpoint; its outstanding failures still
require final-trace inspection.

### 2026-09-06 — Sampling method and qualification scope inspected

Inspected `_stratified_sample` and its public `run_sample` regression: the
22-row population has strata of sizes 1, 1 and 20; a requested six-row sample
contains four rows from the large stratum after capacity redistribution. The
skill describes equal allocation, encounter-order tie breaking, fixed seed 42,
and the requested/actual count distinction. It explicitly withholds claims of
monetary representativeness, confidence levels and defensible extrapolation
weights without a reviewed sampling design. That matches the observed algorithm.

The complete `test_journal_sampling_plugin.py` run is active as session 50886,
with log `sampling-current.log` and intended JUnit `sampling-current.xml`. No
passing result is claimed before terminal completion. The separate integrated
suite session 48596 remains live, at 17% when inspected. Both handles must be
resumed rather than replacing them with new test processes.

### 2026-09-06 — Stratified capacity acceptance expanded

Expanded the existing public stratified sampling regression to three requests
against independently authored strata of sizes 1, 1, 20: request 1 returns 1
(first encountered stratum, zero large-stratum rows); request 6 returns 6 with
four large-stratum rows; request 30 returns the whole 22-row population with all
20 large-stratum rows. Each case also checks requested_size and sample_size in
the public audit. All three passed; Black check passed. Receipt:
`stratified-capacity-current.xml`. No sampling algorithm changed. This adds T03
coverage for more strata than slots and population smaller than request without
claiming inferential validity. The already-running sampling suite was collected
before this test expansion; its eventual receipt and this three-case receipt
must be read together.

### 2026-09-06 — Sampling suite terminal result and repeated MUS hits

The complete sampling test session 50886 finished successfully (exit 0); all
171 collected cases passed, no skips. The test collection predates the additional
stratified parameter cases, whose separate passing receipt is recorded above.
Added a public MUS regression using independently authored journal amounts
+1000, +1 and -1: all three deterministic monetary thresholds hit the dominant
row, producing one unique row. The audit retains requested_size 3, sample_size 1,
population 3 and no random seed. The new case passed separately
(`mus-repeated-current.xml`); Black check passed. This verifies deduplication and
visible numeric shortfall in the audit, not an inferential sampling design or a
claim that three distinct items were reviewed. No production algorithm changed.

### 2026-09-06 — Model-data receipt retries

The 33 existing model-data/report receipt tests passed. Inspected the delayed
receipt case: an injected initial outage retains the request, then successful
stamping writes a supplementary receipt outside sealed outputs, preserving every
original output byte and the request bytes. Added an explicit duplicate-stamp
regression: after one successful synthetic stamp, an identical retry must reuse
the same receipt path and preserve all artifact bytes; a network call fails the
test. It passed, Black check passed, and the combined updated suite passed 34
cases (`model-data-retry-current.xml`). The opener/store are local test doubles;
this does not claim a live server stamp or proof of provider transmission.
No production behavior was changed.

### 2026-09-06 — Workflow registry source drift corrected

The registry consistency test failed on six newly executable
`implementation_bootstrap.py` repair entrypoints in Open Items, Journal Sampling,
Check Entries, Journal Bank, Report Builder and Concordato. Inspected the actual
module-level main guard and argument parser; these are real CLI entrypoints,
not matches inside text or comments. Regenerated the factual workflow registry
from canonical source. No semantic routing policy or host qualification changed.
This source inventory change requires a subsequent package refresh. The broader
integrated test session 48596 remains live and reached 21% at inspection.

### 2026-09-06 — Registry package integration verified

Vera Codex, Cowork and Platform-upload ZIPs were regenerated with the corrected
registry; all three source checks passed. All 18 packaged MCP servers initialized
and listed tools. Registry consistency and the exact Vera upload-card regression
passed together (three tests). Source checkpoint records HEAD
`d4486cdec400b33a0a9b1c75b3ba7fb3e4705b91` and hashes of the four selected changed
runtime/instruction files in `registry-source-checkpoint.json`; this is not an
entire clean-tree attestation. Exact ZIP hashes/byte lengths are retained in
`registry-package-hashes.json`; build log is `registry-package-build.log`.
No artifact was submitted or published. The full-suite session 48596 was verified
live again and had progressed to 23%, with failures still awaiting final traces.

### 2026-09-06 — HTTP partial-response completeness correction

Observed that URL acquisition classified an HTTP 206 response as available with
complete_response whenever it was below the byte cap. Corrected the source
inspector to return partial_capture and partial_response for HTTP 206, without
misrepresenting this as local capture truncation. This is a mechanical HTTP
status boundary, not a semantic source-relevance decision. Expanded the public
inspect_sources socket regression to normal 200 and partial 206 responses; both
passed while retaining numeric-address pinning. The complete source-inspection
suite was started with receipt `http-partial-current.xml`; source/package hashes
must be refreshed for this production change. No deployment authorized or made.

### 2026-09-06 — Partial-response integration and skipped-test correction

Inspection of `http-partial-current.xml` found 64 collected tests with 9 skips:
the initial source-inspection run lacked Node on PATH. It must be described as
55 passes/9 skips, not complete MCP verification. Re-ran with the bundled Node
runtime, producing `http-partial-with-node.xml`; the configured 20-file plugin
Mypy check also passed. All three Vera package formats were rebuilt and passed
source checks, with all 18 packaged MCP servers initializing and listing tools
(`http-partial-build.log`). No publication occurred. The integrated session
48596 remains live and progressed to 25%; it is still the pre-edit baseline.

### 2026-09-06 — Explicit unavailable local sources and pre-read boundary

Removed the source-file existence/type filter that silently omitted explicitly
supplied missing files or directories. Local read failures now retain an
unreadable source record, relative identity when managed, unavailable capture
scope and bounded exception type; no byte digest is invented. Moved the managed
run-path validation before read_bytes, so an outside source is rejected without
reading it. Added public inventory tests for missing files/directories and an
outside-path test whose read_bytes call fails if reached. Full source-inspection
suite passed with Node available; receipt `source-missing-current.xml`. Privacy
hashes refreshed and the full register passed. Packages require regeneration for
this latest source fix; prior package parity is historical, not current.

### 2026-09-06 — Unavailable-source fix packaged

All three Vera package formats were rebuilt with unavailable-source records and
pre-read path validation, and all three source consistency checks passed. All
18 packaged MCP servers initialized/listed tools. Exact hashes and byte sizes
are retained in `source-missing-package-hashes.json`; build log is
`source-missing-build.log`. A focused Isort check found hashlib/importlib ordering
in the receipt test file; corrected only that import order, Black passed and the
duplicate-stamp regression remained green. No release or deployment occurred.
Integrated session 48596 remains live and has advanced to 27%; do not treat any
module/package result as replacing its incomplete diagnostic run.

### 2026-09-06 — Extracted package source-inspection execution

Extracted the current Vera Codex ZIP to a fresh temporary evidence directory and
executed its own public source inspector in an isolated Python subprocess
(`-I -B`). An explicitly missing PDF remained in file_count and sources with an
unreadable disposition and run-relative identity. This verifies packaged code
execution separately from source tests and ZIP byte comparison. Receipt retains
ZIP SHA-256 `75b585eba5e848a33495bfd21b2d8656ae439aff7107ca22a6e66988d14bebf9`
and module hash under `packaged-source-inspection-current/receipt.json`.
This bounded case does not claim a full installed-agent or legal workflow run.
Integrated test session 48596 remains live and was at 28% when polled.

### 2026-09-06 — Case-sensitive source reference correction

Downstream review resolution lowercased every URL/path/source alias, conflating
case-sensitive `/Rule` and `/rule` references. Removed case folding from exact
source identity comparison; semantic relevance remains authored by the reviewer.
Added a public build_audit regression with both URL paths: the uppercase claim
resolves only to source-001 and finds its passage. Full source-inspection/review
suite passed (68 cases, no skips; `source-case-current.xml`). Privacy fingerprint
refreshed and full register passed. Package refresh remains required for this
latest downstream source change. This is source identity correction, not a new
legal-support heuristic.

### 2026-09-06 — Source reference fix package closure

All three Vera formats were regenerated with exact case-sensitive source
reference resolution and passed source consistency checks. All 18 packaged MCP
servers initialized and listed tools. Black/Isort checks on the changed review
module and regression file passed; the configured 20-file plugin Mypy gate
passed. Exact package and review-source hashes are in
`source-case-package-hashes.json`; build log is `source-case-build.log`.
No deployment, merge or publication occurred. The integrated baseline process
48596 remains live at 30% with failures; final traces remain pending.

### 2026-09-06 — Short HTTP body completeness correction

Bounded HTTP reads may return fewer bytes than a declared Content-Length without
raising an exception. The source inspector now records partial_capture with
incomplete_response when the declared response length exceeds acquired bytes
(local byte-cap truncation keeps its separate scope). Added a socket-backed
HTTP 200 response declaring 60 bytes but supplying six; it remains partial
rather than complete. Full component suite passes 69 cases without skips
(`source-short-current.xml`). Privacy source hashes refreshed and register
passed. Package regeneration is pending this additional completeness correction.

### 2026-09-06 — Short-response correction packaged

Codex, Cowork and Platform-upload Vera packages were rebuilt and all source
checks passed with the Content-Length short-response correction. All 18 packaged
MCP servers initialized/listed tools. The configured 20-file Mypy gate and
focused diff whitespace check passed. Exact package hashes are in
`source-short-package-hashes.json`; build log is `source-short-build.log`.
Updated SOURCE_ACQUISITION_ACCEPTANCE.md to the 69-case test receipt and latest
package receipts. No deployment or publication. Integrated session 48596 remains
live at 31%; its incomplete baseline result cannot establish final acceptance.

### 2026-09-06 — Referenced Astra task reconciliation refreshed

Read the latest completed turn of task 01a070ac-a728-7e90-ba29-66554a4c5abb,
whose exact title is `migrate to GPT-6 Astra`. Its latest final remains the
previously recorded PR #525 deployment at e0b98921, with historical test/coverage
and live-page receipts. No newer requested action was found in that turn.
Current AML source includes function-model-data body/copy classes, the shared
model-data script, and the AML navigation context. However, `git merge-base
--is-ancestor e0b98921 HEAD` returned false for local HEAD d4486cdec400b33a0a9b1c75b3ba7fb3e4705b91.
Source presence therefore does not prove commit ancestry or clean integration;
do not claim this dirty checkout contains the release through Git ancestry.
No reset/rebase/merge was performed, preserving concurrent work. The earlier
task's deployment authorization is not authorization to publish these changes.
Integrated session 48596 remains live at 31%.

### 2026-09-06 — Astra source comparison and test reconciliation

Compared 100 non-ZIP files changed by e0b98921 outside generated plugin_packages:
20 matched release bytes, 20 matched pre-release bytes, 57 differed from both,
and three were absent. This is a byte classification, not a semantic verdict;
`astra-source-reconciliation.json` retains each path. Differences were not
blindly overwritten.

Inspected and applied three released test-only fixes after verifying each local
file still exactly matched its pre-release bytes: final-pass filter's single
DataFrame return assertion, readability Document test double, and prejoin-sales
empty resolution-history fixtures that remove accidental database access.
No production or legacy UI code changed; no Git merge occurred. Targeted tests
run under session 32118 with `astra-test-reconciliation.xml`/`.log`; wait for its
terminal result before claiming the reconciliation passes. The integrated suite
started before these test changes and remains baseline evidence only.

The targeted reconciliation process subsequently exited 0: 59 tests passed,
no skips/errors/failures. This closes those three test-file differences only.

### 2026-09-06 — Selective Astra audit-test reconciliation

Inspected three additional release test diffs. Applied only the expected adapter
inventory addition for Archive Organization after verifying the local test still
matched pre-release bytes; the contract-coverage test file passed. Did not copy
two changes that removed no-findings/missing-plugins assertions without first
examining their evidence. Executed the current demo audit: it flags Archive
Organization and Client File Preparation because both use intake-board detail
mode (workflow_detail_mode_duplicate). That shared renderer observation is not
by itself proof of a user-facing defect. The audit's uniqueness assumption needs
review before either changing production UI or suppressing the finding. No
production code or unrelated edits were overwritten.

### 2026-09-06 — Shared renderer audit false positive corrected

Inspected generated renderer code: detailMode is interpolated into a CSS class;
actual workflow content is rendered from adapter-specific detailGroups. Archive
Organization exposes source/destination/duplicate fields, while Client File
Preparation exposes client documents and required evidence. Sharing intake-board
does not collapse those fields or identity. Removed the unsupported global
unique-detailMode audit rule, retaining all per-adapter checks and the repository
no-findings assertion. Added Archive Organization to expected discovery and
changed the duplicate-mode test to require valid shared-style adapters to pass.
All six audit tests passed (`shared-renderer-audit.xml`); Black passed. No shared
renderer, legacy UI, plugin source or package was changed. This addresses the
actual audit assumption instead of merely deleting the release gate assertion.

### 2026-09-06 — Interaction audit findings classified

Executed `audit_plugin_interaction_patterns.py --format json`; retained full
output in `interaction-audit-current.json`. Pattern coverage reports exactly one
missing entry: stateful_decision_review for Lucia's apertura-pratica (19 of 20
applicable components satisfy that pattern). No other pattern has missing
plugins in this run. This is the audit's signal-based observation, not an
independent live review-flow qualification. The upstream test change replacing
empty missing-plugin assertions with list-type assertions remains unapplied:
no Vera implementation gap was established by this current finding, and Lucia
source was not changed to satisfy the broader test. Integrated session 48596
remains live at 32%, with failures awaiting terminal traces.

### 2026-09-06 — Local workbench reconciliation findings

Ran the current workbench server test file: 16 collected, 13 passed, three
failed (`workbench-current.xml`). The registry assertion incorrectly required
all Vera components except Studio Archive to expose this server route. Replaced
it with a nonempty registered-subset assertion, retaining rejection of unknown
workflow IDs; the targeted test passes. This matches the inspected server's
explicit route set, not a claim that every Vera workflow has a browser reviewer.

Two Check Entries fixture tests remain failing because the current MCP renderer
returns no review_payload for their hand-built payloads. The upstream release
deleted them; that deletion was not applied. Their privacy/writeback assertions
must be repaired with a current valid fixture or equivalent integration evidence,
not discarded to make the suite green. No production server change was made.

### 2026-09-06 — Workbench failure diagnosis corrected

Directly inspected the real Check Entries render response for the retained
fixture. It returns ok:true, a bounded model_context_index and opaque
review_reference; the full payload is deliberately in
`result._meta.private_review_payload`. `_mcp_tool_result` returns only
structuredContent, and build_session_payload then demands review_payload there.
Thus the two workbench failures are a transport integration mismatch, not proven
invalid fixtures. The earlier fixture diagnosis is superseded. Preserving the
model-facing reduced response is required; a browser-only adapter would need to
consume component metadata without routing it back into model context. No
production server change was made in this diagnostic step. Do not delete the
privacy/writeback tests or expand the MCP model-facing payload to hide the issue.

### 2026-09-06 — Workbench transport fix verified; package closure pending

Implemented the browser-render-only metadata adapter in
`scripts/serve_review_workbench.py`. `build_session_payload` requests the full
component payload from `_meta.private_review_payload` only after the MCP
structured result reports success. Other tool calls retain the existing
structured response. The browser route still validates plugin and run identity,
then applies the existing absolute-path redaction before exposing the session.
The retained Check Entries privacy and writeback tests now pass without deleting
their assertions or enlarging model-facing MCP context.

Reverified the complete server test file with Node explicitly on PATH: 16 passed
(`workbench-metadata-reverified.xml` and `.log` under the remediation temporary
evidence directory). The focused diff whitespace check passed.

Packaging is not yet closed: the package builder injects this shared server into
eligible component packages. The current privacy validator reports stale reviews
for archive-organization, client-file-preparation, deep-research-validator,
new-client, and prompt-optimizer. Inspect and refresh these affected boundary
reviews before rebuilding and verifying Vera's three package formats. Previous
ZIP parity receipts do not attest this new shared-server source. Integrated test
session 48596 was polled and remains live; its log has advanced to 33% with
failures, so neither integrated success nor current-source qualification is
claimed.

### 2026-09-06 — Shared workbench package and privacy closure

Reviewed the five stale workstream manifests, shared-server fingerprint projection,
runtime profiles, and the affected MCP result builders. These five renderers
still expose their existing structured payloads and do not populate
`private_review_payload`; the new conditional metadata branch therefore preserves
their established data path. Refreshed only their fingerprints. The complete
privacy register now validates successfully; this is an incremental boundary
review, not a new claim about provider retention or local-only processing.

Rebuilt Codex, Claude, and OpenAI upload Vera ZIPs from current source; all three
package checks passed and the Claude check initialized and listed all 18 MCP
servers. `workbench-package-build.log`, `workbench-package-check.log`, and
`workbench-package-receipt.json` in the remediation temporary evidence directory
record the results and hashes. Compared injected server bytes: five matching
copies in Codex, five in Claude, ten in the upload ZIP. Claude's only difference
from the shared server is the inspected Codex-to-Claude runtime docstring
projection; its executable fix matches. This supersedes the preceding pending
package note. No deployment or publication occurred. Integrated session 48596
remains live at 34% with failures; broad qualification remains incomplete.

### 2026-09-06 — Missing-support XML scenario reverified

Extended the existing public Check Entries missing-support request regression
to cover an unrelated FatturaPA XML in the supplied support directory, as well
as an empty directory, in Italian, French, and German. This directly exercises
the previously reported XML-run defect rather than relying only on an empty
support population. The output still identifies missing support and requests
a localized supporting document without imposing PDF format. Together with
the supported and unresolved XML narrative cases, all 15 cases pass with no
skips (`xml-request-reverified.xml` and `.log` in the temporary evidence
directory). Black and focused whitespace checks pass. This turn changed tests
only; the source wording fix and package receipts remain as previously recorded.
It does not establish full report localization or professional acceptance.

### 2026-09-06 — T07 process-level crash and concurrent launch evidence

Added public `ensure_runtime` regressions using separate Python processes on
this macOS host. One process exits abruptly while holding the installation lock
after creating a synthetic unfinished environment. It publishes no active
pointer; the next launch successfully acquires the abandoned lock and publishes
a distinct generation. A second case launches two processes against the same
target and verifies both receive the same ready generation, with exactly one
generation directory and one active pointer. These supplement the existing
thread-level concurrency, timeout, and failed-replacement tests.

The complete managed-runtime file passes: 41 tests, no skips
(`runtime-current.xml` and `.log` under the remediation temporary evidence
directory). Black and focused whitespace checks pass. Install subprocesses are
synthetic; the lock, interpreter processes, private directories, and atomic
publication are real. This is macOS process-level evidence, not Windows
execution or a real dependency-download qualification. No runtime production
source or package changed in this step.

### 2026-09-06 — T07 resolved-dependency receipt evidence

Added two public installation cases with the same published requirement range
and distinct synthetic installed distribution metadata (1.0 and 1.1). The ready
receipt records each exact resolved version, interpreter version, and runtime
key, rather than reporting the requirement constraint as the installed version.
All 43 managed-runtime tests pass with no skips
(`runtime-resolution-current.xml` and `.log`). Dependency metadata and install
commands are synthetic; filesystem metadata inspection and receipt creation use
the production implementation. No package resolution or supported-host claims
are inferred from these tests. Integrated session 48596 remains live at 42%,
including failures and setup errors whose terminal traces are not yet available.

### 2026-09-06 — Sales Plan natural-output acceptance scope corrected

Read the complete current Sales Plan workflow contract: its natural delivery is
the scenario CSV, assumption ledger, summary, reconciliation, prepared-evidence
manifest, model-use manifest, and execution receipt. It does not generate
HTML/PDF/XLSX. The earlier Sales Plan-specific rendered-report acceptance note
is therefore not a requirement of this workflow; T16's visual requirement still
applies wherever the other workflows actually produce rendered reports.

Inspected the retained standalone execution output. All six receipt-listed
artifact hashes and sizes match; all four implementation-file hashes match
current source. Independent Decimal aggregation of its eight scenario rows
verifies every Actual, Plan, delta, and rounded percentage in all 36 summary
rows (144 numeric cells). No Plan engine was imported for this verification.
Evidence: `sales-plan-summary-current.json` in the remediation temporary evidence
directory. The earlier independent source/assumption-to-row checks remain
separate evidence. The run correctly retains `report_ready=false`; this does
not confer professional approval of the synthetic assumptions or qualify all
remaining T16 edge cases.

### 2026-09-06 — Shared Business Planning integration regression

Ran both current Business Planning test files together: 82 passed, no skips
(`business-planning-current.xml` and `.log` in the temporary evidence directory).
Inspected the assertions for incomplete scenario isolation, funding across the
full horizon, adjusted versus client-reported EBITDA, and reviewed KPI basis.
The partial scenario does not suppress the independent complete scenario; the
funding statement binds to the full-horizon calculation; a reported EBITDA
reference cannot bypass the adjusted-result guard. A supported KPI rubric is
explicitly a monitored hypothesis, not a rating scale. These are source-backed
contract regressions, not independent professional endorsement of economic
viability or a claim that readiness means healthy economics. No shared Vera or
Clara production source was changed. Integrated session 48596 remains live;
terminal failure traces are still pending.


## 2026-09-06 integrated diagnostic completed; Astra Medium handoff

Session 48596 finished with exit 1: 9532 passed, 103 failed, 19 errors,
34 skipped; coverage 79.50%, below 80%. See INTEGRATED_DIAGNOSTIC.md for the
complete module inventory, current Open Item and accounting reproductions,
remaining unknowns, and ordered remediation steps. This baseline spans edits and
is not final qualification. The user is being pinged to select Astra Medium before
remaining remediation; no model change is claimed. T01–T20 remain incomplete.


## 2026-09-06 resumed after user confirmation of Astra Medium

The user explicitly confirmed the model setting and requested resumption. Corrected
22 Open Item MCP source-expansion fixtures to inject unauthorized executable source,
both before startup and between requests, preserving rejection on all 11 public
surfaces. Separate timestamp-valid malicious-bytecode non-execution tests remain.
Updated the Italian Journal–Bank case's missing-support expectations. Updated the
Check Entries credit-note assertion to retain observed XML gross amount 11 while
verifying signed values -10/-11 and differences -1/1, including prepared evidence.
The focused batch passed 26 tests with no skips or failures, recorded in
`/private/tmp/vera-remediation-01a07083/remediation-resumed-accounting.xml`.
Changes are test-only; no production behavior or package publication changed.
Black identified existing formatting in the touched Open Item test file; applied
those local formatting corrections. The two other touched test files pass Black.
The broader diagnostic inventory and T01–T20 qualification gaps remain open.


## 2026-09-06 workbench inventory expectations reconciled

Inspected 11 canonical workbench adapters, including Archive Organization. Current
focused audit suite reproduced five stale count/list assertions. Updated only the
live inventory expectations (11 adapters/contracts and alphabetically ordered
11-plugin generated templates); retained historical example-specific assertions.
All 49 tests across readiness, evidence-bundle generation, demo auditing and payload
coverage pass with no skips. Evidence: `inventory-final.xml` under the retained
private diagnostic directory. Black checks passed for both changed test files.
No audit rules, severity thresholds or production contracts were relaxed.


## 2026-09-06 current browser, lifecycle and privacy batch

Current batch: 260 passed, 2 failed, no skips (262 total), recorded in
`/private/tmp/vera-remediation-01a07083/current-browser-registry-privacy.xml`.
Browser capability wording already reflects Settings > Computer Use > Google Chrome
and the review-server suite passes. Remaining failures: the test-local lifecycle
classification tuples omit implementation_bootstrap.py for six components; the
separate generated workflow registry does not supply those tuples. Studio Archive's
privacy fingerprint is currently stale and requires source review before refresh.
These are current unresolved findings; earlier registry/package receipts do not
close them. Updated the top checkpoint to reflect the completed diagnostic and
user-confirmed Astra Medium resumption.


## 2026-09-06 lifecycle classification corrected

Inspected all six implementation_bootstrap.py CLI guards: each requires --repair,
activates its implementation boundary and repairs its own vendor bytecode; none
opens a customer workflow from that entry point. Added the six maintenance CLIs
to the test-local allowlist, retaining exact one-classification coverage. The
previously failing coverage test passes (`lifecycle-classification-fixed.xml`);
Black passes the changed file. Studio Archive privacy review is still in progress:
current changes include session-bound tool execution and imported-filename
provenance; no fingerprint refresh has yet been applied for this review.


Studio Archive incremental review: inspected the session identity/lock/configuration
wrapper and filename receipt-to-input-binding path. The existing filename-lineage
privacy control describes the sealed imported-name register and immutable run
snapshots. Session-specific default state, cross-session ownership rejection and
configuration-change detection tests pass (4 tests, no skips;
`studio-session-review.xml`). The session isolation control still needs recording
in the privacy manifest; complete boundary/page review and package refresh remain.


## 2026-09-06 Studio Archive privacy and local packages current

Completed incremental source/privacy review of session configuration ownership,
process locking, pinned configuration return checks and imported filename lineage.
Recorded session-owned-archive-configuration in the manifest; existing model-context
and filename-lineage records cover imported names. The public final model-data block
continues to describe selected IDs, paths, names and opened evidence. Refreshed only
Studio Archive; the complete Vera privacy validator passes. Full Studio Archive,
Vera privacy and lifecycle batch: 271 passed, no skips or failures, retained in
`studio-privacy-lifecycle-final.xml`. All three Vera local ZIPs rebuilt and passed
source parity checks; 18 packaged MCP servers initialized and listed tools.
Logs: `studio-privacy-package-build.log`, `studio-privacy-package-check.log`;
exact SHA-256/size receipt: `studio-privacy-package-hashes.json` in the same retained
private diagnostic directory. These are local package results, not publication.
The broader package/update suite and final integrated qualification remain open.


## 2026-09-06 broader package/update result

390 tests completed in 67.236 seconds: 387 passed, 3 failed, no skips.
Evidence: `package-updates-current.xml` and `.log` in the retained diagnostic
folder. Both extracted Clara render failures occur at dependency installation
with NameResolutionError for pypi.org; they do not establish a renderer defect.
The update failure is public Clara 0.1.177 versus installed 0.1.180. Source/ZIP
parity failures from the baseline no longer reproduce in this suite. No public
manifest edit or deployment was made. These three failed gates remain explicit.


## 2026-09-06 extracted-package renderer network retry

Retried exactly the two extracted Clara rendering cases with approved network
access for declared managed-runtime dependencies. Both passed, with no skips,
errors or failures (`extracted-render-network.xml` and `.log`). The earlier DNS
failures were environmental; no renderer code or test assertion changed.
Combined with the 387 passing cases in package-updates-current.xml, the remaining
package/update finding is Clara's public-version manifest mismatch. This combined
result is not represented as a single fresh 390-test green run.


## 2026-09-06 Centrale Rischi gold-source recovery

Downloaded three public reference candidates into centrale-source-recovery in
the retained private acceptance directory. Banca d'Italia's guida-centrale-rischi.pdf
exactly matches the fixed official_plain_guide SHA-256
0360543e706b0ea8ff3c2109b1bd93cec0712025eb0ebc2a70dfe0b51553466e.
This recovers one of four previously missing gold sources; its negative-layout
benchmark has not yet been executed. The candidate detailed guide and historical
examples do not match their fixed source hashes and are not accepted as replacements.
Exact URLs, hashes and match results are in centrale-source-recovery/recovery.json.
No gold values or expected source hashes were changed.


## 2026-09-06 recovered Centrale Rischi negative control passed

Ran the canonical benchmark runner on the exact-hash official_plain_guide source,
using a separately recorded selection manifest without modifying canonical gold
facts. All 24 pages produced zero records in all eight financial populations and
zero unsupported-data candidates. The one negative control passes; the runner
reports deterministic_pass_semantic_review_pending, which does not qualify the
unexecuted cases. Evidence: centrale-source-recovery/negative-run/benchmark_receipt.json,
negative-manifest.json and negative-selection.json. Three other original source
PDFs still lack exact-hash recovery; no replacements were silently substituted.


## 2026-09-06 official Centrale Rischi guide recovered and replayed

The Italian 41-page detailed guide downloaded from Banca d'Italia exactly matches
canonical official_guide SHA-256 b5e2b059ff482a2ec142ac7901f09f4d392fbe2191ffaf86ad14dae05a1a307e.
The earlier nonmatching candidate was the English-prefixed URL. Replayed all
14 existing extraction cases and 10 analysis cases for this official guide,
plus the recovered plain guide negative control: 15/15 extraction/control and
10/10 analysis pass. No expected values, source hashes or mappings were edited.
Receipt remains deterministic_pass_semantic_review_pending; outputs are retained
under centrale-source-recovery/official-run, with selection provenance in
official-selection.json and official-manifest.json. This is educational-source
numeric qualification, not professional acceptance of a client report. Historical
examples and camera_webinar exact-hash sources remain missing; synthetic_report
has separately retained acceptance evidence.


Centrale Rischi source recovery follow-up: the historical candidate is a different
14-page edition, while the fixed historical benchmark requires 17 pages; both page
count and hash reject it. Targeted local PDF-name searches and inspection of the
benchmark-introduction commit did not identify the original historical or webinar
source path. Public candidate searches did not yet recover either exact source.
Keep these two cases unqualified; do not manufacture source PDFs or alter gold
hashes to obtain a pass. Other remediation work can continue independently.


## 2026-09-06 original diagnostic failure replay launched

Selected every unique failed/errored test from integrated-failures.json against
current source. First collection exited 4 because two tests were renamed; no
execution result is inferred from that attempt. Inspected and mapped the current
Business Planning parity and Vera review-route test names, preserving that mapping
in diagnostic-failure-replay-current-selection.json. Corrected replay is live in
session 99499; log diagnostic-failure-replay-current.log and eventual JUnit .xml.
Poll this exact handle; do not rerun solely because the wait expires. Network and
loopback restrictions may still appear and require separate scoped qualification.


## 2026-09-06 diagnostic replay and catalog update verified

Session 99499 completed: 122 tests, 84 passed, 38 failed, no errors or skips.
Evidence: diagnostic-failure-replay-current.xml and remaining-diagnostic-failures.json
in the retained diagnostic directory. The earlier launch entry is superseded.
The guarded local catalog refresh changed only version fields: Vera 0.1.206 to
0.1.207 and Clara 0.1.173 to 0.1.180, matching inspected source manifests.
Catalog/package verification passes (1 test, zero failures/errors/skips, 6.157s);
catalog-refresh.json and catalog-refresh.xml retain the exact result. This is
local generated metadata, not publication or public-version confirmation.


## 2026-09-06 A04 outcome measurements

Measured retained pre/post-fix managed outputs against explicit source rows:
missed required links fell from 2/2 to 0/2; corrected output has 0 unexpected
links out of 2 predictions, zero unmatched rows and exact EUR1830 allocation.
Technical reviewer changed 0/2 proposed links. ACCOUNTING_REVIEW_CASES.md now
records denominators and independent-label limits; exact source/output hashes
are in journal-bank-a04-outcome-metrics.json. No new runtime or professional
qualification is claimed by this measurement.


### Passive Invoice missing-support managed acceptance — 2026-09-06

Executed the public run_audit.py CLI with a Studio Archive engagement, one
synthetic FatturaPA XML invoice (INV-1, EUR122, supplier Alfa), a booked movement
for a different supplier and invoice (UNRELATED-2, EUR122, supplier Beta), and
an explicit canonical numeric/header mapping. Chunk size25, concurrency2; no
semantic packets are eligible because no invoice matches. The dependency check
still reports the native Luna worker unqualified; no worker boundary was bypassed.

Initial runtime correctly produced one unmatched invoice, one ledger orphan and
professional_review_required, but left the XLSX professional_should_inspect
cell blank. That acceptance is recorded failed, with all original files retained
under passive-missing-support-current in the private evidence directory.

Corrected source retains an operational missing-support request in both JSONL
and XLSX: inspect the supplied scope and mapping, request the corresponding
movement or an explanation of its absence from the extract, and do not create
a booking from the screening result. This explains the exact no-match outcome;
it does not decide that a booking was omitted or alter semantic judgments.

A fresh managed CLI run, run_60db21717bfa72c383951469, uses byte-identical sources.
Direct output checks verify 1 unmatched invoice, 1 orphan, 0 matches, 0 semantic
chunks and identical request text in JSONL/XLSX. Eight output artifacts, including
SQLite and an Italian model-data disclosure, are sealed to ready_for_review.
Evidence: passive-missing-support-fixed/source-output-verification.json,
execution-result.json, finalize-result.json and model-data-request.json beneath
/private/tmp/vera-remediation-01a07083. The main model saw synthetic source and
output excerpts; zero worker chunks does not mean wholly local model processing.

Component regression: 89 passed, 1 opt-in native test skipped, 0 failed/errors
(passive-source-request.xml). No independent professional, native visual or
semantic-worker qualification is claimed. This case adds actual missing-support
runtime evidence; it does not cover matched-invoice account-treatment judgment.


Passive Invoice source-request packaging: all three local Vera ZIPs rebuilt and
passed source-parity checks; 18 packaged MCP servers initialized/listed tools.
Exact SHA256/size receipt: passive-request-package-hashes.json. The complete
privacy register passes after the reviewed output-only change; model packet,
external destination and host qualification behavior are unchanged.
Combined package/update/privacy test run: 418 tests, 415 passed, 3 failed,
zero skips/errors, 69.196s (passive-request-package-tests.xml). Failures remain
the two extracted Clara dependency DNS cases and public Clara 0.1.177 versus
installed 0.1.180. Earlier authorized network retries provide separate passing
evidence for the two render cases; this invocation is not called green.
No publication or public-version change occurred.


### Passive Invoice ambiguous candidates — 2026-09-06

Public managed CLI run with one invoice and two independently identified booked
movements M1/M2 correctly withheld matching and semantic review. Its normal XLSX
failed technical acceptance: candidate IDs were only in JSONL and the reviewer
next-action cell was blank. Original outputs remain under passive-ambiguous-current,
with a failed acceptance lifecycle record.

The output now retains candidate movement IDs, source ledger references and exact
supporting match fields in both JSONL and XLSX. Ambiguous and duplicate-candidate
states receive explicit evidence requests; these instructions do not select a
movement, infer an omitted booking or treat a copied XML as a second obligation.
The ambiguous finding now describes nonunique allocation rather than incorrectly
requiring multiple candidates (one movement can be contested by multiple invoices).
No worker prompt or match-selection rule changed.

Fresh managed run run_1d648d60925264560ce61143 uses byte-identical input files.
Direct verification confirms both M1/M2 references visible, one unresolved
ambiguous invoice, zero selected matches, zero ledger orphans, zero worker chunks,
and identical action text in JSONL/XLSX. Eight artifacts, including Italian
model-data disclosure, are sealed for review. Exact hashes and assertions:
/private/tmp/vera-remediation-01a07083/passive-ambiguous-fixed/source-output-verification.json.
This is technical source/output evidence; independent professional, native visual
and worker-host acceptance remain unqualified.

Component regressions: 91 passed, 1 opt-in native skip, zero failures/errors
(passive-candidates.xml). The new normal-output regression also verifies that two
ledger candidates remain visible for duplicated XML documents. Duplicate-source
managed CLI acceptance is not inferred from that regression. All three Vera ZIPs
rebuilt and passed parity with 18 packaged MCP startups; exact package hashes
are in passive-candidates-package-hashes.json. Privacy register passes; the local
candidate evidence adds no worker/model destination. No publication occurred.

Selected Vera/privacy/Cowork integrity tests: 35 tests, 0 failures, 0 errors, 0 skips (passive-candidates-integrity.xml).


### Passive Invoice duplicate XML managed execution — 2026-09-06

Executed the actual public CLI under Studio Archive with a ZIP containing two
byte-identical XML members (one.xml and two.xml), one three-line booked movement
M1, and the explicit canonical header/number mapping. No fixture runner or model
substitute was supplied to the command. Both XML members retain distinct source
references and invoice identities; both are duplicate_candidate with M1 shown
only as a candidate. Results: 2 duplicate flags, 0 selected matches, 0 orphans,
0 semantic chunks. Both workpaper rows include the evidence request and M1's
source ledger reference. No second obligation or allocation is inferred.

Managed run run_64ca19420c29876307961d47 is sealed for review with 10 outputs,
including staged XML, SQLite, JSONL, normal XLSX and Italian model-data disclosure.
Evidence: /private/tmp/vera-remediation-01a07083/passive-duplicate-current/
source-output-verification.json, execution-result.json and finalize-result.json.
This closes the managed duplicate-source execution missing from the earlier
regression-only checkpoint. Independent professional and native visual review,
matched-invoice semantic review and unsupported-host qualification remain open.


Astra lifecycle test reconciliation: reread the latest completed turn of
`migrate to GPT-6 Astra`; it still reports PR525/e0b98921 and no newer work.
Inspected its test-only replacement of the brittle 49-route total with the
required /api/vera/run-receipts presence check. Current hosted_services/api.py
registers that router; all retired-route absence and required-family assertions
remain. Applied the released test bytes only after checking the local file was
byte-identical to its pre-release version. No production code or Git ancestry
changed. Full lifecycle module: 21 tests, 0 failures, 0 errors, 0 skips
(astra-lifecycle-reconciliation.xml). This resolves that diagnostic test failure;
it does not import the other task's coverage result into this checkout.


## ON current funding notice added to source inventory — 6 September 2026

Directly rechecked the official ON normative index and followed its notice link:
[Invitalia, notice of 30 June 2026](https://www.invitalia.it/news-media/news/oltre-nuove-imprese-tasso-zero-dal-1deg-luglio-2026-disponibile-solo-il-finanziamento-agevolato).
The notice states that new applications from 1 July 2026 receive subsidized
finance only because the grant allocation is exhausted. It says the measure
remains active; this does not establish the eligibility of either synthetic case.
Any September funding description must account for this notice separately from
the participant and company-age conditions tested by L03/L04.

Original HTML retained (115170 bytes; SHA256
ed38ea001b3bfc77758cbed1b9a951fece30c32900290a461cc6789de08522af), with URL,
retrieval timestamp, status and two exact paragraph text/hash records, under
/private/tmp/vera-remediation-01a07083/professional-primary-sources/
on-financing-notice-2026-06-30.html and on-financing-notice-excerpts.json.
This adds a previously absent current source; it is not an exhaustive amendment
search or professional approval. Existing sealed FAQ/formal-source runs have
not been silently expanded. A subsequent source-set revision must import it
before relying on it in a normal bounded contribution.

Fresh restricted model contributions remain pending authorization to use
sub-agents; the existing task must not be labelled a fresh source-restricted
session. Approved L01–L08 substantive expectations remain approved.


T13 trace-payload qualification: current scan identifies 14 identical ordinary
review-session helpers. 112 isolated comparisons preserve payload/error behavior
for a candidate pure constructor. File I/O and output collection remain local;
no production extraction is claimed. RESPONSIBILITY_MAP.md records exact scope,
source hashes, experiment and remaining packaged/direct-import qualification.


## Shared trace constructor implemented — 6 September 2026

Extracted build_review_execution_step into the existing shared assurance
serialization module for Check Entries, Journal Sampling, Journal–Bank,
Concordato and Report Builder. These five public entrypoints already initialize
that package. Their local readers, writers and output collectors remain intact;
the shared helper constructs only the previously identical trace fields.
Output collection still follows input/command conversion, preserving error order.

Before implementation, all 10 direct/public-packaged startup probes found the
existing module. After rebuilding, all10 probes call the new helper successfully.
112 isolated comparisons of the production constructor against the unchanged
original helper preserve payload and exception behavior. AST comparison confirms
all pre-existing serialization logic is unchanged. These are bounded proofs,
not substitutes for the full workflow regressions now running.

The five helper bodies shrink from130 to65 lines; the shared function adds19,
for84 lines total (46 fewer). Other helpers retain their existing implementation
until their own startup paths are qualified. Vera ZIP size increases by6332 bytes
(Codex),6427 bytes (Cowork) and6338 bytes (upload): the shared module is replicated
in packaged component trees. This is a maintenance reduction in canonical code,
not a package-size reduction. No vendored dependency was pruned.

23 affected Vera privacy fingerprints refreshed after the output-boundary review;
the complete register passes. All9 local Vera/Clara/Lucia ZIPs rebuilt and passed
parity checks. Clara's privacy validator also passes. Lucia has no standalone
privacy CLI at the assumed path; its existing opening-matter fingerprint failure
is separate and has not been refreshed without source review. No public version,
deployment, merge or publication was performed.

Evidence under /private/tmp/vera-remediation-01a07083:
shared-trace-boundary-review.json, shared-trace-production-differential.json,
trace-shared-import-probe/result.json, trace-shared-import-after/result.json,
shared-trace-size-comparison.json and shared-trace-package-hashes.json.

The full six-module assurance/workflow regression remains active in session27113,
with shared-trace-regression.log and eventual shared-trace-regression.xml. Do not
restart it on an observation timeout or claim it passed before terminal evidence.


## Trace extraction extended to nine supported workflows — 6 September 2026

A second direct/packaged probe found the existing assurance module usable by
Client File Preparation, Deep Research Validator, Prompt Optimizer and Variance
Analysis. These four now use the same pure trace constructor. Distribution,
Mix Contribution, Period Comparison, Scatter Bubble and Set Overlap fail that
additional module import in both source and package probes; their ordinary
startup completed first. This establishes a missing shared dependency for the
proposed extraction, not broken ordinary workflow startup. They remain unchanged.
No new search path or bootstrap dependency was introduced merely to remove lines.

Nine caller functions plus the shared helper total136 lines versus234 before
(98 fewer). File I/O, output-reference collection and workflow naming remain
local. The production helper passes112 comparisons against the archived original
function, now explicitly loaded from the pre-change extracted package so the
comparison cannot silently become self-comparison after adoption.

Deep Research Validator, Prompt Optimizer and Variance suites:169 passed, no
skips/errors/failures. Client File Preparation initially60 passed/16 Node skips;
all16 skipped MCP cases then passed with Node on PATH. This is combined evidence,
not a single fresh76-test run. The original six-module run in session27113 is
still active; do not restart it. No terminal outcome is claimed for that run.

All9 local package builds/parity checks pass after this extension; exact hashes
are in shared-trace-nine-workflows-package-hashes.json. Vera and Clara privacy
registers pass. The local public ZIP parity failure remains specifically Clara:
its builder intentionally disallows public promotion without the separate real
Cowork/Linux acceptance gate in check_clara_cowork_release.py. Vera and Lucia
public ZIP bytes match their candidates. Do not copy Clara's candidate to its
public path or relax the test to hide this unfulfilled release condition.
The separate catalog-test attempt failed because Node was absent from PATH;
its corrected exact rerun is tracked separately, not inferred from that failure.

Evidence under /private/tmp/vera-remediation-01a07083:
trace-shared-import-remaining/result.json, shared-trace-additional-regression.xml,
shared-trace-file-preparation.xml, shared-trace-file-preparation-native.xml,
shared-trace-nine-workflows-size.json and shared-trace-nine-workflows-package.log.

Corrected exact catalog check with Node on PATH passed (1/1, no skips/errors).
Evidence: shared-trace-catalog-node.xml. The earlier Node-missing failure remains
historical environmental evidence; Clara public promotion is still separate.


Trace extraction verification follow-up: 90 isolated comparisons of all nine
current wrappers against the archived original preserve exact read/output/write
order, written payload and exception type. Cases include malformed JSON,
nonobject/null inputs, null command, output-read failure, write failure, explicit
empty local-file posture and pre-existing trace replacement. No output read
occurs after malformed intake and no write follows failed output collection.
Evidence: qualify_trace_io_order.py and shared-trace-io-order.json.

The configured unsuppressed 20-source-file Mypy check passes; Bandit reports no
medium/high issue in the ten changed trace/shared source files. Logs:
shared-trace-mypy.log and shared-trace-bandit.log. Isort found an existing
hashlib/json order issue at the top of Journal Sampling review_session.py. Its
fix is intentionally deferred until session27113 completes so the source is
not changed during the active suite. No full-repository gate closure is claimed.


Trace helper coverage: coverage.py reports no missing statements or branches
inside build_review_execution_step under112 differential cases. This is changed-
function coverage only, not whole-module coverage and not an update to79.50%
repository coverage. Evidence: shared-trace-helper-coverage.json and the retained
coverage database/full JSON.

Current computer-use probe: cua.getState returned Mac locked / automatic unlock
failed. Manual unlock requested asynchronously; browser acceptance remains
pending, with no bypass. Exact timestamp/result: browser-access-current.json.
The prior unlocked observation is superseded for current access. Source/test
work can continue independently.


## Shared trace full regression completed — 6 September 2026

Session27113 exited0: 1158 passed, no skips/errors/failures,1435.327 seconds.
Evidence: shared-trace-regression.xml/log. This supersedes every prior running
entry for that handle; do not poll or restart it. Together with the separately
retained169 additional-workflow passes and60+16 File Preparation passes, the
extracted nine-workflow boundary has1403 passing test results across batches.
This is not the full repository collection and does not replace its coverage gate.

Applied the queued hashlib/json import sorting only after terminal evidence.
All eleven changed trace/source/test files pass Isort; Journal Sampling passes
Black. Its source fingerprint and three Vera candidate ZIPs are being refreshed
with post-formatting parity checks and one actual sampling-artifact smoke test.
Those follow-up processes have their own handles/logs; do not infer their result
from the1158-test result.


Post-formatting closure: the actual Journal Sampling artifact/workbook smoke
test passes1/1 with no skips, and all three refreshed Vera ZIPs pass parity
checks (including18 packaged MCP startup/tool-list checks). The complete Vera
privacy register passes. Exact files: shared-trace-final-sampling-smoke.xml,
shared-trace-final-privacy.log, shared-trace-final-vera-package.log and
shared-trace-final-vera-package-hashes.json. The queued import-order issue is
resolved. No trace-regression process remains active.

This closes the implemented nine-workflow trace extraction's local verification.
It does not establish whole-repository coverage, professional/native-host
acceptance, or proof that unobserved vendored code is unreachable. The original
T01–T20 scope and other recorded qualification gaps remain open.

### 2026-09-06 — Further reconciliation of Astra test expectations

Inspected current production behavior before applying test-only corrections. For
`tests/src/slides/test_ocr_payload.py`, `tests/scripts/test_build_png_examples_gallery.py`,
and `tests/test_saloncentric_discovery.py`, verified the complete local file matched
`e0b98921^` before taking the already released `e0b98921` test revision. OCR preserves
individual list items; gallery labels replace hyphens with spaces; SalonCentric rank
bands use 20% and 50% cumulative cutoffs and the permanent taxonomy now contains four
attribute families. Explicit alternate-family coverage remains in the revised test.

In `tests/scripts/test_build_spanish_video_editions.py`, removed only the assertion
that a repository-derived absolute path cannot contain `/Users/fabio/`. Retained
both repository-relative derivation and environment override assertions. No production
files changed in this batch; no legacy UI work performed.

Verification with repository venv and bundled Node PATH: three OCR/gallery/video
modules **66 passed, zero failures/errors/skips** (8.579 s); SalonCentric module
**12 passed, zero failures/errors/skips** (3.150 s). Evidence:
`/private/tmp/vera-remediation-01a07083/astra-three-test-reconciliation.xml` and
`astra-saloncentric-reconciliation.xml`, with corresponding logs. These close eight
failures from the prior 38-failure replay; they do not establish full-suite success
or improve the recorded 79.50% full-suite coverage by assertion. Goal remains active.

### 2026-09-06 — Remaining Astra expectation reconciliation and import finding

Applied the released `e0b98921` test-only revisions to homepage compliance,
website typography and retailer category evidence-pack tests after verifying
all three local files exactly matched `e0b98921^`. Inspected current five-language
homepage strings, both shared CSS selector groups and bundle-key component parsing.
The fixture now supplies its actual structured Terrace/Logo Detail key; production
classification was not changed. **120 tests passed, zero failures/errors/skips**
(16.096 s), recorded in `astra-final-expectation-reconciliation.xml` and its log
under `/private/tmp/vera-remediation-01a07083/`.

The Clara case-bound deck import is a distinct unresolved issue, not a stale
expectation: an isolated interpreter loading the builder with its script directory
on sys.path reproduces `ModuleNotFoundError: No module named 'case_store'` in
`advisory_evidence_lineage.py`. The documented builder command supplies `--case-dir`;
its lineage loader does not establish the sibling scripts dependency path.
Traceback: `clara-lineage-isolated-import.log`. No test monkeypatch or production
change was made. Repository instructions prohibit production edits to resolve test
import failures; this issue remains explicitly reported.

`diagnostic-reconciliation-ledger.json` matches individual test names/classes to
saved passing JUnit cases and hashes their receipts. Of the prior 38 failures,
21 have separate passing evidence and 17 remain unclosed. Some passing evidence
uses an authorized network/loopback environment; it is not evidence that restricted
environment runs pass. Remaining: nine legacy UI tests (excluded from development
by repository instruction), four Lucia tests, the Clara import, Clara public-version
lag, Clara public ZIP promotion mismatch, and the interaction audit for apertura-pratica
workflow metadata. This is not a fresh full-suite pass or 80% coverage proof.

### 2026-09-06 — Managed Sales Plan sparse cross-year acceptance

Executed the public `run_plan.py` CLI in Studio Archive run
`run_91880f13e558e50b57b93299`, using three imported synthetic Actual rows in
December 2024 and March 2025, mapped to December 2025 and March 2026. Germany
has no March row. Case/source staging in the run output is byte-identical to
sealed imported inputs and recorded in `staging-provenance.json`.

The normal seven Plan outputs contain exactly three Actual and three Plan rows,
with no imputed months or Germany March row. Independent Decimal calculations
from the source CSV verify all 60 numeric cells and every source/target period
without importing the Plan engine. Reconciliation discloses one missing
grain-period with `sparse_source_time_profile`; it and the execution receipt
retain `report_ready=false`. Default model-use metadata excludes raw Actuals
and full scenario rows. This technical evaluation did inspect the six-row
synthetic output in Codex, and the Italian model-data report discloses that.

All 11 physical artifacts (seven Plan outputs, two prepared inputs, two
model-data reports) were declared, sealed, validated and the technical run
marked completed. Completion is execution lifecycle status, not approval of the
forecast. Expected assumptions are authored synthetic fixtures, not independent
professional review. No source changes were needed and no publication occurred.
Evidence directory: `/private/tmp/vera-remediation-01a07083/sales-sparse-boundary/`,
including execution, source-output-verification, finalize and complete receipts.
Driver scripts: `run_sales_sparse_boundary.py`, `verify_finalize_sales_sparse.py`.

### 2026-09-06 — Sales Plan missing-discount defect reproduced and corrected

Four separate managed public CLI runs reject blank, NaN, Infinity and `1,234`
gross-sales values with exit 2 and an identified source row/field. None writes
a scenario or success receipt. Each failed run and imported source is retained;
`sales-invalid-boundaries.json` records the exact errors and hashes.

A separate valid-schema run with a blank discount exposed a production arithmetic
defect: the Actual row reported net sales 900 and margin 360 despite the unknown
discount; summary aggregation also skipped missing values and emitted partial
sums as totals. Corrected canonical `plugins/sales-plan/scripts/prepare_sales_plan_case.py`
to propagate unavailable discount into net sales and margin, and unavailable
members into their affected summary totals/deltas. Explicit zero remains known.
Unaffected gross sales and complete groups remain calculable. A source-row-linked
warning identifies each missing optional metric. Mechanical null propagation is
justified by exact arithmetic, not semantic classification.

All 29 Sales Plan tests pass, zero failures/errors/skips, including five new
public-run regressions for missing discount/COGS/units and explicit zero discount.
`run_540acde8f14f7726664344dd` reruns the original blank-discount inputs through the
managed public CLI; byte-identical source now leaves dependent values and totals
blank while gross sales remains 900. Differential evidence:
`/private/tmp/vera-remediation-01a07083/sales-missing-discount-differential.json`.
Original incorrect run `run_78584f0a18252b14a4b0a0c2` is retained, not approved.

Remaining for this correction: review the changed privacy fingerprint and exact
warning data path, regenerate affected packages, run package integrity checks,
and seal/close the before/after run lifecycle with honest model-data reports.
This checkpoint does not claim package parity or final acceptance.

### 2026-09-06 — Sales Plan 0.1.7 local integration closure

Reviewed the changed source-to-warning-to-model path. Reconciliation is a default
model artifact; missing-metric warnings expose metric names and exact affected
source row IDs. The Sales Plan privacy record and full skill now state this
explicitly, retain no automatic anonymization, and explain unavailable dependent
values/summary totals and the distinction from explicit zero. Complete Vera
privacy validation passes after the reviewed fingerprint refresh.

Sales Plan component version is now 0.1.7. All three Vera package variants were
regenerated after the final skill/manifest edits and pass source parity; all 18
Cowork MCP servers initialize and list tools. Latest identities are retained in
`sales-017-final-identities.json`, final builder log in
`sales-017-final-packages.log`. Black, Isort and changed-source Bandit checks pass.

The broader required package/update suite completed: 385 passed, 5 failed,
zero errors/skips (57.929 s). The component-version failure is corrected and its
exact regression passes. Two Clara render failures reproduce the recorded PyPI
DNS restriction. Clara public version remains behind its installed cache.
The remaining source/ZIP drift is confined to Clara distribution_core.py and
reporting-engine semantic_layer.schema.json, compile_execution.py and
semantic_layer.py; these outside-scope edits were preserved and not regenerated
into a release. No claim of full package-suite success or public release.

Corrected managed run run_540acde8f14f7726664344dd now has 11 sealed/validated
artifacts, an Italian model-data report and completed technical lifecycle.
The original run run_78584f0a18252b14a4b0a0c2 is marked failed, with its incorrect
outputs retained. Synthetic technical review is not professional forecast
approval. Evidence: sales-missing-discount-after/{finalize-result,complete-result}.json
and sales-missing-discount-before/failed-run-result.json under the remediation
temporary evidence root. T01–T20 remain active; this closes local integration
of this specific numeric correction only.

### 2026-09-06 — Sales Plan CI gate and deliberate-regression qualification

The inspected dedicated plugin correctness workflow did not execute Sales Plan.
Added a separate Ubuntu/Python 3.12/Node 22 job running its complete test module
with declared requirements, bytecode disabled and an explicit 80% preparation-engine
coverage gate in `tests/plugin_sales_plan.coveragerc`. Corrected the MCP test to
compare the advertised version to the actual component manifest after the 0.1.7
bump, rather than its stale 0.1.6 literal. No production source was changed.

Exact CI test command passes locally: 29 tests, zero failures/errors/skips;
86.06% statement coverage of the 581-statement preparation engine (500 covered).
Initial CI-style attempt found the stale version assertion; it is retained in
`sales-ci-qualification.log`. Passing evidence is `sales-ci-final.xml/log`.
This is local command evidence, not a hosted Actions run or whole-repository coverage.

Three isolated temporary source copies qualify regression detection. The clean
copy passes all five selected missing-value/zero controls. Reintroducing the old
missing-discount-as-zero calculation fails two tests; allowing partial summary
totals fails four. All failures are assertions, with zero import/setup errors.
The production source SHA-256 was checked before/after and is unchanged. Evidence:
`/private/tmp/vera-remediation-01a07083/sales-regression-detection/result.json` and
variant-specific JUnit/logs; driver `qualify_sales_regression_detection.py`.
This supplies concrete T09 regression-detection evidence for the T16 correction.
Broader T01–T20 acceptance and the previously recorded external gaps remain open.

### 2026-09-06 — Sales Plan added to shared type and security gates

Added the changed preparation engine to `tests/plugin_runtime.mypy.ini`, which
also supplies the CI Bandit target list. The exact expanded type command passes
all 21 source files with `ignore_errors=False`; Bandit scans 34,591 lines and
reports zero medium/high findings (five low findings remain). No production
changes or package regeneration were needed for this gate configuration change.

Evidence: `/private/tmp/vera-remediation-01a07083/sales-expanded-types.log` and
`sales-expanded-security.log`. The initial one-file CLI attempt used an unsupported
Mypy flag and stopped before analysis; the corrected explicit configuration and
expanded final configuration both passed. This expands targeted checks, not
whole-repository coverage, hosted CI proof or professional acceptance.

### 2026-09-06 — Measured retained negative-case outcomes

Inspected the actual final reconciliation JSON and source-qualification records
for A05 (`source-acceptance-a05-final`), A06
(`source-acceptance-a06-candidate-fixed`, superseding the earlier output-fixed
candidate), and A07 (`source-acceptance-a07-reviewed-current`). The three runs
contain four open-item rows: all four abstain from settlement, all retain an
evidence request, none has a matched-evidence ID and all allocation ledgers are
empty. A06 retains the expected failed bank relationship control; this is a
correctly withheld settlement, not an all-green professional run.

Recorded exact output hashes, mapped source refs, row/page IDs, status counts
and failed controls in `/private/tmp/vera-remediation-01a07083/open-item-negative-outcome-metrics.json`.
The executable measurement script is `measure_open_item_abstentions.py` in the
same root. This measures retained runtime evidence; it does not claim a fresh
execution or current-source replay. User-approved qualitative expectations are
retained, but no independent complete edge-label/candidate-decision dataset is
available for these runs. Accordingly independent false-match, missed-match and
reviewer-correction rates remain null, not fabricated zeroes. These measurements
complement the earlier A04 required-link and conservation measurements.

### 2026-09-06 — Bandi four-source candidate set registered

Prepared a fresh Studio Archive run `run_68fcec44e931f9671e78ff59` in the existing
synthetic L03 engagement, preserving earlier runs. Imported and registered the
retained FAQ, decree, circular and financing notice using the shipped initialize
and register CLIs. All four bound source hashes match their actual imported bytes;
all remain `candidate`. The notice is registered as `other`/`clarifying`, not as
an invented formal statutory amendment. No review confirmation or model-session
attestation was manufactured.

Evidence: `bandi-l03-current/funding-notice-context-location.json`,
`funding-notice-source-verification.json`, and `bandi-funding-source-set.log`
under `/private/tmp/vera-remediation-01a07083/`. Source-set revision is 4.
The notice's publication/effective dates are retained in its acquisition receipt;
register date fields are unset, and legacy title/issuer labels remain provisional.
Source interpretation must resolve that metadata before professional approval.
Fresh-session bounded interpretation and independent professional acceptance
remain pending. This is actual source registration, not completed Bandi analysis.

### 2026-09-06 — Additional Centrale Rischi source search

Inspected the Camera di Commercio Messina publication page and its linked
`oliani_161122.pdf`: the web PDF reader reports 19 pages, incompatible with the
38-page camera_webinar gold source. Excluded without substitution. The Rome
training page exposes a PPT and did not identify the required PDF. Historical
search returned the previously excluded alternative edition; it was not fetched
again. Neither missing original was recovered. Dispositions and exact URLs are
in `centrale-source-recovery/additional-source-search.json`. Further closure
requires the original files or their acquisition locations; benchmark hashes
remain unchanged. No new benchmark pass is claimed.

### 2026-09-06 — Bandi metadata transcription corrected

Read the retained PDF first-page titles/issuer headings and HTML headings.
A fresh four-source candidate run replaces provisional filenames/issuer labels
with source-backed descriptions. The decree register uses its GU publication
date 2021-01-27; the financing notice uses retained publication 2026-06-30 and
effective date 2026-07-01. The circular's issued date remains in its title;
its publication/effective dates and unknown FAQ dates are not invented.

All four source bytes/hashes are identical to the preceding candidate set.
The immutable preceding register is unchanged and its now-superseded preparation
run is explicitly cancelled. All new sources remain candidate; operator metadata
transcription is not professional source-baseline approval. Exact new run ID,
hash checks and disposition: `bandi-l03-current/registered-metadata-verification.json`;
context: `metadata-reviewed-context-location.json`. Fresh-session contributions
and professional acceptance remain pending. The current worker prerequisite
recheck separately still reports unsupported with no canary or worker execution
(`current-host-final-prerequisite-check.log`).

### 2026-09-06 — Goal blocked after three consecutive impasse checks

No new authorization for fresh Bandi sessions, source originals, host access or
professional output decisions arrived during the three consecutive checks.
The current Bandi run remains needs_review with zero intelligence contributions
and zero review events. Worker prerequisites remain unsupported. Further local
regression passes cannot establish those external acceptance requirements.
Goal status is blocked, not complete; no requirement is waived. Preserve the
working changes and resume from STATO_E_PROSSIMI_PASSI_IT.md when the necessary
input/access changes. No merge, deployment, publication or destructive cleanup
was performed. The full objective remains T01–T20.
