# Clara 0.1.194 and Lucia 0.1.42 — reusable desktop teaching

The release adds Impara con Clara and Impara con Lucia, using one shared local
stdlib lifecycle derived from Vera. First use includes an interview and 3–4
current specialist workflows; later sessions can teach a workflow again without
resetting the profile. Native OpenAI voice belongs to the teaching chat, while a
second visible native chat runs bounded steps and returns actual artifacts.
Clara and Lucia retain separate OS-user profiles and reject cross-product state.
No custom voice/model API or Mparanza tutorial transport is introduced.

Clara's OpenAI projection now preserves complete specialist instructions and
references so teaching can follow the current workflow rather than a short
marketplace card. Cowork omits both learning skills, helpers, fixtures and gates.
Vera's runtime and Cowork bytes are unchanged; its shared-page privacy review
was refreshed and the repository archive hash recorded separately from its
already published archive.

## Verification before submission

- Focused regression: 569 passed, 2 skipped.
- Shared lifecycle: 33 tests, 86.89% statement coverage; each engine above 80%.
- Two actual product runs: Clara's bound fictional workbook produces EUR 9,000
  aggregate EBITDA variance; Lucia's bound fictional brief produces the real
  matter-opening memo and review payload with professional decisions pending.
- Eight package/page tests verify independent archive execution, source-preserving
  OpenAI teaching, absence from Cowork and five translated public pages.
- Skills validate; black, isort, mypy and bandit pass for the shared runtime.
- Packages build from source with real MCP startup checks.
- Italian pages inspected in the native browser; two-chat copy and the working
  product-directory breadcrumb verified. The shared renderer remains unchanged.

Four public architecture failures were reproduced on the clean db896831 source:
missing treasury breadcrumb, an overbroad Bandi copy slice, an existing standalone
Vera page count and an existing model-data paragraph count. No unrelated page or
expectation was changed to conceal them.

Automated native IDs and participant confirmations are test fixtures. They do not
prove a live spoken lesson or two physically visible native chats. The skill
requires actual host evidence or user confirmation for voice/window setup and
never manufactures user participation or professional approval.

## Publication verified on 2026-09-13

PR #616 merged at `6e56e27c4b50a63aae6fd5c5999dd807216faf56` after all 20
GitHub checks passed. CI portability fixes also passed 83 targeted tests. The
server deployed that commit with a clean worktree and the new Uvicorn process;
12 live page, navigation and ZIP artifacts matched their repository SHA-256.

OpenAI Platform now shows Clara **0.1.194 Published** and Lucia **0.1.42 Published**.
All 16 Clara and 10 Lucia skills passed the automated scans. After the user
confirmed the displayed terms and publisher declarations, both submissions and
Publish Version actions completed. Each independent public directory shows the
new version and its Impara con skill. Exact submission URLs and archive hashes
are in `marketplace-publications.json`.

The public Clara update notice now advertises 0.1.194. Rebuilding its package to
refresh the governed notice changes only
`privacy/hosted-services/plugin-update-check.json`; the exact published hash and
the later repository hash are recorded separately. No executable or teaching
instruction changed. Lucia's repository and published archive are identical.
The earlier Lucia draft remains unused; no previous draft was deleted.
